#Requires -RunAsAdministrator
<#
.SYNOPSIS
    CIS Windows Hardening Script
.DESCRIPTION
    Consolidated hardening script for 248 CIS rules.
    Generated by CIS Hardening Platform.

    WARNING: Review each rule before applying to production systems.
#>

param(
    [switch]$WhatIf
)

$ErrorActionPreference = 'Stop'
$Script:Results = @()

function Write-RuleResult {
    param([string]$RuleId, [string]$Title, [string]$Status, [string]$Details)
    $Script:Results += [PSCustomObject]@{
        RuleId = $RuleId; Title = $Title; Status = $Status; Details = $Details
    }
    $color = switch ($Status) {
        'PASS' { 'Green' }
        'FAIL' { 'Red' }
        'APPLIED' { 'Cyan' }
        default { 'Yellow' }
    }
    Write-Host "[${Status}] ${RuleId}: ${Title}" -ForegroundColor $color
    if ($Details) { Write-Host "       $Details" -ForegroundColor Gray }
}

# ── CIS 2.3.1.1 ──────────────────────────────────────
function Apply-CIS_2_3_1_1 {
    <#
    .SYNOPSIS
        Ensure 'Accounts: Guest account status' is set to 'Disabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.1.1" -Title "Ensure 'Accounts: Guest account status' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        $guest = Get-LocalUser | Where-Object { $_.SID -like '*-501' }; if ($guest) { Disable-LocalUser -Name $guest.Name }
        Write-RuleResult -RuleId "2.3.1.1" -Title "Ensure 'Accounts: Guest account status' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.1.1" -Title "Ensure 'Accounts: Guest account status' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.1.2 ──────────────────────────────────────
function Apply-CIS_2_3_1_2 {
    <#
    .SYNOPSIS
        Ensure 'Accounts: Limit local account use of blank passwords to console logon only' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.1.2" -Title "Ensure 'Accounts: Limit local account use of blank passwords to console logon only' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa' -Name 'LimitBlankPasswordUse' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.1.2" -Title "Ensure 'Accounts: Limit local account use of blank passwords to console logon only' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.1.2" -Title "Ensure 'Accounts: Limit local account use of blank passwords to console logon only' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.1.3 ──────────────────────────────────────
function Apply-CIS_2_3_1_3 {
    <#
    .SYNOPSIS
        Configure 'Accounts: Rename administrator account'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.1.3" -Title "Configure 'Accounts: Rename administrator account'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        $admin = Get-LocalUser | Where-Object { $_.SID -like '*-500' }; if ($admin -and $admin.Name -eq 'Administrator') { Rename-LocalUser -Name $admin.Name -NewName 'RenamedAdmin' }
        Write-RuleResult -RuleId "2.3.1.3" -Title "Configure 'Accounts: Rename administrator account'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.1.3" -Title "Configure 'Accounts: Rename administrator account'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.1.4 ──────────────────────────────────────
function Apply-CIS_2_3_1_4 {
    <#
    .SYNOPSIS
        Configure 'Accounts: Rename guest account'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.1.4" -Title "Configure 'Accounts: Rename guest account'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        $guest = Get-LocalUser | Where-Object { $_.SID -like '*-501' }; if ($guest -and $guest.Name -eq 'Guest') { Rename-LocalUser -Name $guest.Name -NewName 'RenamedGuest' }
        Write-RuleResult -RuleId "2.3.1.4" -Title "Configure 'Accounts: Rename guest account'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.1.4" -Title "Configure 'Accounts: Rename guest account'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.10.1 ──────────────────────────────────────
function Apply-CIS_2_3_10_1 {
    <#
    .SYNOPSIS
        Ensure 'Network access: Allow anonymous SID/Name translation' is set to 'Disabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.10.1" -Title "Ensure 'Network access: Allow anonymous SID/Name translation' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa' -Name 'LsaAnonymousNameLookup' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.10.1" -Title "Ensure 'Network access: Allow anonymous SID/Name translation' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.10.1" -Title "Ensure 'Network access: Allow anonymous SID/Name translation' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.10.10 ──────────────────────────────────────
function Apply-CIS_2_3_10_10 {
    <#
    .SYNOPSIS
        Ensure 'Network access: Restrict clients allowed to make remote calls to SAM' is set to 'Administrators: Remote Access: Allow'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.10.10" -Title "Ensure 'Network access: Restrict clients allowed to make remote calls to SAM' is set to 'Administrators: Remote Access: Allow'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa' -Name 'RestrictRemoteSAM' -Value 'O:SYG:SYD:(A;;RC;;;BA)' -Type String -Force
        Write-RuleResult -RuleId "2.3.10.10" -Title "Ensure 'Network access: Restrict clients allowed to make remote calls to SAM' is set to 'Administrators: Remote Access: Allow'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.10.10" -Title "Ensure 'Network access: Restrict clients allowed to make remote calls to SAM' is set to 'Administrators: Remote Access: Allow'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.10.11 ──────────────────────────────────────
function Apply-CIS_2_3_10_11 {
    <#
    .SYNOPSIS
        Ensure 'Network access: Shares that can be accessed anonymously' is set to 'None'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.10.11" -Title "Ensure 'Network access: Shares that can be accessed anonymously' is set to 'None'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\LanManServer\Parameters' -Name 'NullSessionShares' -Value @() -Type MultiString -Force
        Write-RuleResult -RuleId "2.3.10.11" -Title "Ensure 'Network access: Shares that can be accessed anonymously' is set to 'None'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.10.11" -Title "Ensure 'Network access: Shares that can be accessed anonymously' is set to 'None'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.10.12 ──────────────────────────────────────
function Apply-CIS_2_3_10_12 {
    <#
    .SYNOPSIS
        Ensure 'Network access: Sharing and security model for local accounts' is set to 'Classic - local users authenticate as themselves'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.10.12" -Title "Ensure 'Network access: Sharing and security model for local accounts' is set to 'Classic - local users authenticate as themselves'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa' -Name 'ForceGuest' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.10.12" -Title "Ensure 'Network access: Sharing and security model for local accounts' is set to 'Classic - local users authenticate as themselves'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.10.12" -Title "Ensure 'Network access: Sharing and security model for local accounts' is set to 'Classic - local users authenticate as themselves'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.10.2 ──────────────────────────────────────
function Apply-CIS_2_3_10_2 {
    <#
    .SYNOPSIS
        Ensure 'Network access: Do not allow anonymous enumeration of SAM accounts' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.10.2" -Title "Ensure 'Network access: Do not allow anonymous enumeration of SAM accounts' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa' -Name 'RestrictAnonymousSAM' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.10.2" -Title "Ensure 'Network access: Do not allow anonymous enumeration of SAM accounts' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.10.2" -Title "Ensure 'Network access: Do not allow anonymous enumeration of SAM accounts' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.10.3 ──────────────────────────────────────
function Apply-CIS_2_3_10_3 {
    <#
    .SYNOPSIS
        Ensure 'Network access: Do not allow anonymous enumeration of SAM accounts and shares' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.10.3" -Title "Ensure 'Network access: Do not allow anonymous enumeration of SAM accounts and shares' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa' -Name 'RestrictAnonymous' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.10.3" -Title "Ensure 'Network access: Do not allow anonymous enumeration of SAM accounts and shares' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.10.3" -Title "Ensure 'Network access: Do not allow anonymous enumeration of SAM accounts and shares' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.10.4 ──────────────────────────────────────
function Apply-CIS_2_3_10_4 {
    <#
    .SYNOPSIS
        Ensure 'Network access: Do not allow storage of passwords and credentials for network authentication' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.10.4" -Title "Ensure 'Network access: Do not allow storage of passwords and credentials for network authentication' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa' -Name 'disabledomaincreds' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.10.4" -Title "Ensure 'Network access: Do not allow storage of passwords and credentials for network authentication' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.10.4" -Title "Ensure 'Network access: Do not allow storage of passwords and credentials for network authentication' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.10.5 ──────────────────────────────────────
function Apply-CIS_2_3_10_5 {
    <#
    .SYNOPSIS
        Ensure 'Network access: Let Everyone permissions apply to anonymous users' is set to 'Disabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.10.5" -Title "Ensure 'Network access: Let Everyone permissions apply to anonymous users' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa' -Name 'EveryoneIncludesAnonymous' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.10.5" -Title "Ensure 'Network access: Let Everyone permissions apply to anonymous users' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.10.5" -Title "Ensure 'Network access: Let Everyone permissions apply to anonymous users' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.10.6 ──────────────────────────────────────
function Apply-CIS_2_3_10_6 {
    <#
    .SYNOPSIS
        Ensure 'Network access: Named Pipes that can be accessed anonymously' is set to 'None'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.10.6" -Title "Ensure 'Network access: Named Pipes that can be accessed anonymously' is set to 'None'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\LanManServer\Parameters' -Name 'NullSessionPipes' -Value @() -Type MultiString -Force
        Write-RuleResult -RuleId "2.3.10.6" -Title "Ensure 'Network access: Named Pipes that can be accessed anonymously' is set to 'None'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.10.6" -Title "Ensure 'Network access: Named Pipes that can be accessed anonymously' is set to 'None'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.10.7 ──────────────────────────────────────
function Apply-CIS_2_3_10_7 {
    <#
    .SYNOPSIS
        Ensure 'Network access: Remotely accessible registry paths' is configured
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.10.7" -Title "Ensure 'Network access: Remotely accessible registry paths' is configured" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\SecurePipeServers\winreg\AllowedExactPaths' -Name 'Machine' -Value @('System\CurrentControlSet\Control\ProductOptions','System\CurrentControlSet\Control\Server Applications','Software\Microsoft\Windows NT\CurrentVersion') -Type MultiString -Force
        Write-RuleResult -RuleId "2.3.10.7" -Title "Ensure 'Network access: Remotely accessible registry paths' is configured" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.10.7" -Title "Ensure 'Network access: Remotely accessible registry paths' is configured" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.10.8 ──────────────────────────────────────
function Apply-CIS_2_3_10_8 {
    <#
    .SYNOPSIS
        Ensure 'Network access: Remotely accessible registry paths and sub-paths' is configured
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.10.8" -Title "Ensure 'Network access: Remotely accessible registry paths and sub-paths' is configured" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\SecurePipeServers\winreg\AllowedPaths' -Name 'Machine' -Value @('System\CurrentControlSet\Control\Print\Printers','System\CurrentControlSet\Services\Eventlog','Software\Microsoft\OLAP Server','Software\Microsoft\Windows NT\CurrentVersion\Print','Software\Microsoft\Windows NT\CurrentVersion\Windows') -Type MultiString -Force
        Write-RuleResult -RuleId "2.3.10.8" -Title "Ensure 'Network access: Remotely accessible registry paths and sub-paths' is configured" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.10.8" -Title "Ensure 'Network access: Remotely accessible registry paths and sub-paths' is configured" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.10.9 ──────────────────────────────────────
function Apply-CIS_2_3_10_9 {
    <#
    .SYNOPSIS
        Ensure 'Network access: Restrict anonymous access to Named Pipes and Shares' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.10.9" -Title "Ensure 'Network access: Restrict anonymous access to Named Pipes and Shares' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\LanManServer\Parameters' -Name 'RestrictNullSessAccess' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.10.9" -Title "Ensure 'Network access: Restrict anonymous access to Named Pipes and Shares' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.10.9" -Title "Ensure 'Network access: Restrict anonymous access to Named Pipes and Shares' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.11.1 ──────────────────────────────────────
function Apply-CIS_2_3_11_1 {
    <#
    .SYNOPSIS
        Ensure 'Network security: Allow Local System to use computer identity for NTLM' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.11.1" -Title "Ensure 'Network security: Allow Local System to use computer identity for NTLM' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa' -Name 'UseMachineId' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.11.1" -Title "Ensure 'Network security: Allow Local System to use computer identity for NTLM' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.11.1" -Title "Ensure 'Network security: Allow Local System to use computer identity for NTLM' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.11.10 ──────────────────────────────────────
function Apply-CIS_2_3_11_10 {
    <#
    .SYNOPSIS
        Ensure 'Network security: Minimum session security for NTLM SSP based (including secure RPC) clients' is set to 'Require NTLMv2 session security, Require 128-bit encryption'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.11.10" -Title "Ensure 'Network security: Minimum session security for NTLM SSP based (including secure RPC) clients' is set to 'Require NTLMv2 session security, Require 128-bit encryption'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa\MSV1_0' -Name 'NTLMMinClientSec' -Value 537395200 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.11.10" -Title "Ensure 'Network security: Minimum session security for NTLM SSP based (including secure RPC) clients' is set to 'Require NTLMv2 session security, Require 128-bit encryption'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.11.10" -Title "Ensure 'Network security: Minimum session security for NTLM SSP based (including secure RPC) clients' is set to 'Require NTLMv2 session security, Require 128-bit encryption'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.11.11 ──────────────────────────────────────
function Apply-CIS_2_3_11_11 {
    <#
    .SYNOPSIS
        Ensure 'Network security: Minimum session security for NTLM SSP based (including secure RPC) servers' is set to 'Require NTLMv2 session security, Require 128-bit encryption'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.11.11" -Title "Ensure 'Network security: Minimum session security for NTLM SSP based (including secure RPC) servers' is set to 'Require NTLMv2 session security, Require 128-bit encryption'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa\MSV1_0' -Name 'NTLMMinServerSec' -Value 537395200 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.11.11" -Title "Ensure 'Network security: Minimum session security for NTLM SSP based (including secure RPC) servers' is set to 'Require NTLMv2 session security, Require 128-bit encryption'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.11.11" -Title "Ensure 'Network security: Minimum session security for NTLM SSP based (including secure RPC) servers' is set to 'Require NTLMv2 session security, Require 128-bit encryption'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.11.12 ──────────────────────────────────────
function Apply-CIS_2_3_11_12 {
    <#
    .SYNOPSIS
        Ensure 'Network security: Restrict NTLM: Audit Incoming NTLM Traffic' is set to 'Enable auditing for all accounts'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.11.12" -Title "Ensure 'Network security: Restrict NTLM: Audit Incoming NTLM Traffic' is set to 'Enable auditing for all accounts'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa\MSV1_0' -Name 'AuditReceivingNTLMTraffic' -Value 2 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.11.12" -Title "Ensure 'Network security: Restrict NTLM: Audit Incoming NTLM Traffic' is set to 'Enable auditing for all accounts'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.11.12" -Title "Ensure 'Network security: Restrict NTLM: Audit Incoming NTLM Traffic' is set to 'Enable auditing for all accounts'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.11.13 ──────────────────────────────────────
function Apply-CIS_2_3_11_13 {
    <#
    .SYNOPSIS
        Ensure 'Network security: Restrict NTLM: Outgoing NTLM traffic to remote servers' is set to 'Audit all' or higher
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.11.13" -Title "Ensure 'Network security: Restrict NTLM: Outgoing NTLM traffic to remote servers' is set to 'Audit all' or higher" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa\MSV1_0' -Name 'AuditNTLMInDomain' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.11.13" -Title "Ensure 'Network security: Restrict NTLM: Outgoing NTLM traffic to remote servers' is set to 'Audit all' or higher" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.11.13" -Title "Ensure 'Network security: Restrict NTLM: Outgoing NTLM traffic to remote servers' is set to 'Audit all' or higher" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.11.2 ──────────────────────────────────────
function Apply-CIS_2_3_11_2 {
    <#
    .SYNOPSIS
        Ensure 'Network security: Allow LocalSystem NULL session fallback' is set to 'Disabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.11.2" -Title "Ensure 'Network security: Allow LocalSystem NULL session fallback' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa\MSV1_0' -Name 'allownullsessionfallback' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.11.2" -Title "Ensure 'Network security: Allow LocalSystem NULL session fallback' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.11.2" -Title "Ensure 'Network security: Allow LocalSystem NULL session fallback' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.11.3 ──────────────────────────────────────
function Apply-CIS_2_3_11_3 {
    <#
    .SYNOPSIS
        Ensure 'Network Security: Allow PKU2U authentication requests to this computer to use online identities' is set to 'Disabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.11.3" -Title "Ensure 'Network Security: Allow PKU2U authentication requests to this computer to use online identities' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa\pku2u' -Name 'AllowOnlineID' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.11.3" -Title "Ensure 'Network Security: Allow PKU2U authentication requests to this computer to use online identities' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.11.3" -Title "Ensure 'Network Security: Allow PKU2U authentication requests to this computer to use online identities' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.11.4 ──────────────────────────────────────
function Apply-CIS_2_3_11_4 {
    <#
    .SYNOPSIS
        Ensure 'Network security: Configure encryption types allowed for Kerberos' is set to 'AES128_HMAC_SHA1, AES256_HMAC_SHA1, Future encryption types'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.11.4" -Title "Ensure 'Network security: Configure encryption types allowed for Kerberos' is set to 'AES128_HMAC_SHA1, AES256_HMAC_SHA1, Future encryption types'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\Software\Microsoft\Windows\CurrentVersion\Policies\System\Kerberos\Parameters' -Name 'SupportedEncryptionTypes' -Value 2147483640 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.11.4" -Title "Ensure 'Network security: Configure encryption types allowed for Kerberos' is set to 'AES128_HMAC_SHA1, AES256_HMAC_SHA1, Future encryption types'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.11.4" -Title "Ensure 'Network security: Configure encryption types allowed for Kerberos' is set to 'AES128_HMAC_SHA1, AES256_HMAC_SHA1, Future encryption types'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.11.5 ──────────────────────────────────────
function Apply-CIS_2_3_11_5 {
    <#
    .SYNOPSIS
        Ensure 'Network security: Do not store LAN Manager hash value on next password change' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.11.5" -Title "Ensure 'Network security: Do not store LAN Manager hash value on next password change' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa' -Name 'NoLMHash' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.11.5" -Title "Ensure 'Network security: Do not store LAN Manager hash value on next password change' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.11.5" -Title "Ensure 'Network security: Do not store LAN Manager hash value on next password change' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.11.7 ──────────────────────────────────────
function Apply-CIS_2_3_11_7 {
    <#
    .SYNOPSIS
        Ensure 'Network security: LAN Manager authentication level' is set to 'Send NTLMv2 response only. Refuse LM & NTLM'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.11.7" -Title "Ensure 'Network security: LAN Manager authentication level' is set to 'Send NTLMv2 response only. Refuse LM & NTLM'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa' -Name 'LmCompatibilityLevel' -Value 5 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.11.7" -Title "Ensure 'Network security: LAN Manager authentication level' is set to 'Send NTLMv2 response only. Refuse LM & NTLM'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.11.7" -Title "Ensure 'Network security: LAN Manager authentication level' is set to 'Send NTLMv2 response only. Refuse LM & NTLM'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.11.8 ──────────────────────────────────────
function Apply-CIS_2_3_11_8 {
    <#
    .SYNOPSIS
        Ensure 'Network security: LDAP client encryption requirements' is set to 'Negotiate sealing' or higher
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.11.8" -Title "Ensure 'Network security: LDAP client encryption requirements' is set to 'Negotiate sealing' or higher" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\LDAP' -Name 'LDAPClientIntegrity' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.11.8" -Title "Ensure 'Network security: LDAP client encryption requirements' is set to 'Negotiate sealing' or higher" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.11.8" -Title "Ensure 'Network security: LDAP client encryption requirements' is set to 'Negotiate sealing' or higher" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.11.9 ──────────────────────────────────────
function Apply-CIS_2_3_11_9 {
    <#
    .SYNOPSIS
        Ensure 'Network security: LDAP client signing requirements' is set to 'Negotiate signing' or higher
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.11.9" -Title "Ensure 'Network security: LDAP client signing requirements' is set to 'Negotiate signing' or higher" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\NTDS\Parameters' -Name 'LDAPServerIntegrity' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.11.9" -Title "Ensure 'Network security: LDAP client signing requirements' is set to 'Negotiate signing' or higher" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.11.9" -Title "Ensure 'Network security: LDAP client signing requirements' is set to 'Negotiate signing' or higher" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.14.1 ──────────────────────────────────────
function Apply-CIS_2_3_14_1 {
    <#
    .SYNOPSIS
        Ensure 'System cryptography: Force strong key protection for user keys stored on the computer' is set to 'User is prompted when the key is first used' or higher
    .NOTES
        Category:       Local Policies
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.14.1" -Title "Ensure 'System cryptography: Force strong key protection for user keys stored on the computer' is set to 'User is prompted when the key is first used' or higher" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\Software\Policies\Microsoft\Cryptography' -Name 'ForceKeyProtection' -Value 2 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.14.1" -Title "Ensure 'System cryptography: Force strong key protection for user keys stored on the computer' is set to 'User is prompted when the key is first used' or higher" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.14.1" -Title "Ensure 'System cryptography: Force strong key protection for user keys stored on the computer' is set to 'User is prompted when the key is first used' or higher" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.15.1 ──────────────────────────────────────
function Apply-CIS_2_3_15_1 {
    <#
    .SYNOPSIS
        Ensure 'System objects: Require case insensitivity for non-Windows subsystems' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.15.1" -Title "Ensure 'System objects: Require case insensitivity for non-Windows subsystems' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Session Manager\Kernel' -Name 'ObCaseInsensitive' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.15.1" -Title "Ensure 'System objects: Require case insensitivity for non-Windows subsystems' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.15.1" -Title "Ensure 'System objects: Require case insensitivity for non-Windows subsystems' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.15.2 ──────────────────────────────────────
function Apply-CIS_2_3_15_2 {
    <#
    .SYNOPSIS
        Ensure 'System objects: Strengthen default permissions of internal system objects (e.g. Symbolic Links)' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.15.2" -Title "Ensure 'System objects: Strengthen default permissions of internal system objects (e.g. Symbolic Links)' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Session Manager' -Name 'ProtectionMode' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.15.2" -Title "Ensure 'System objects: Strengthen default permissions of internal system objects (e.g. Symbolic Links)' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.15.2" -Title "Ensure 'System objects: Strengthen default permissions of internal system objects (e.g. Symbolic Links)' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.17.1 ──────────────────────────────────────
function Apply-CIS_2_3_17_1 {
    <#
    .SYNOPSIS
        Ensure 'User Account Control: Admin Approval Mode for the Built-in Administrator account' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.17.1" -Title "Ensure 'User Account Control: Admin Approval Mode for the Built-in Administrator account' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System' -Name 'FilterAdministratorToken' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.17.1" -Title "Ensure 'User Account Control: Admin Approval Mode for the Built-in Administrator account' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.17.1" -Title "Ensure 'User Account Control: Admin Approval Mode for the Built-in Administrator account' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.17.2 ──────────────────────────────────────
function Apply-CIS_2_3_17_2 {
    <#
    .SYNOPSIS
        Ensure 'User Account Control: Behavior of the elevation prompt for administrators in Admin Approval Mode' is set to 'Prompt for consent on the secure desktop' or higher
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.17.2" -Title "Ensure 'User Account Control: Behavior of the elevation prompt for administrators in Admin Approval Mode' is set to 'Prompt for consent on the secure desktop' or higher" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System' -Name 'ConsentPromptBehaviorAdmin' -Value 2 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.17.2" -Title "Ensure 'User Account Control: Behavior of the elevation prompt for administrators in Admin Approval Mode' is set to 'Prompt for consent on the secure desktop' or higher" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.17.2" -Title "Ensure 'User Account Control: Behavior of the elevation prompt for administrators in Admin Approval Mode' is set to 'Prompt for consent on the secure desktop' or higher" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.17.3 ──────────────────────────────────────
function Apply-CIS_2_3_17_3 {
    <#
    .SYNOPSIS
        Ensure 'User Account Control: Behavior of the elevation prompt for standard users' is set to 'Automatically deny elevation requests'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.17.3" -Title "Ensure 'User Account Control: Behavior of the elevation prompt for standard users' is set to 'Automatically deny elevation requests'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System' -Name 'ConsentPromptBehaviorUser' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.17.3" -Title "Ensure 'User Account Control: Behavior of the elevation prompt for standard users' is set to 'Automatically deny elevation requests'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.17.3" -Title "Ensure 'User Account Control: Behavior of the elevation prompt for standard users' is set to 'Automatically deny elevation requests'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.17.4 ──────────────────────────────────────
function Apply-CIS_2_3_17_4 {
    <#
    .SYNOPSIS
        Ensure 'User Account Control: Detect application installations and prompt for elevation' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.17.4" -Title "Ensure 'User Account Control: Detect application installations and prompt for elevation' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System' -Name 'EnableInstallerDetection' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.17.4" -Title "Ensure 'User Account Control: Detect application installations and prompt for elevation' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.17.4" -Title "Ensure 'User Account Control: Detect application installations and prompt for elevation' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.17.5 ──────────────────────────────────────
function Apply-CIS_2_3_17_5 {
    <#
    .SYNOPSIS
        Ensure 'User Account Control: Only elevate UIAccess applications that are installed in secure locations' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.17.5" -Title "Ensure 'User Account Control: Only elevate UIAccess applications that are installed in secure locations' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System' -Name 'EnableSecureUIAPaths' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.17.5" -Title "Ensure 'User Account Control: Only elevate UIAccess applications that are installed in secure locations' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.17.5" -Title "Ensure 'User Account Control: Only elevate UIAccess applications that are installed in secure locations' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.17.6 ──────────────────────────────────────
function Apply-CIS_2_3_17_6 {
    <#
    .SYNOPSIS
        Ensure 'User Account Control: Run all administrators in Admin Approval Mode' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.17.6" -Title "Ensure 'User Account Control: Run all administrators in Admin Approval Mode' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System' -Name 'EnableLUA' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.17.6" -Title "Ensure 'User Account Control: Run all administrators in Admin Approval Mode' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.17.6" -Title "Ensure 'User Account Control: Run all administrators in Admin Approval Mode' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.17.7 ──────────────────────────────────────
function Apply-CIS_2_3_17_7 {
    <#
    .SYNOPSIS
        Ensure 'User Account Control: Switch to the secure desktop when prompting for elevation' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.17.7" -Title "Ensure 'User Account Control: Switch to the secure desktop when prompting for elevation' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System' -Name 'PromptOnSecureDesktop' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.17.7" -Title "Ensure 'User Account Control: Switch to the secure desktop when prompting for elevation' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.17.7" -Title "Ensure 'User Account Control: Switch to the secure desktop when prompting for elevation' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.17.8 ──────────────────────────────────────
function Apply-CIS_2_3_17_8 {
    <#
    .SYNOPSIS
        Ensure 'User Account Control: Virtualize file and registry write failures to per-user locations' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.17.8" -Title "Ensure 'User Account Control: Virtualize file and registry write failures to per-user locations' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System' -Name 'EnableVirtualization' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.17.8" -Title "Ensure 'User Account Control: Virtualize file and registry write failures to per-user locations' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.17.8" -Title "Ensure 'User Account Control: Virtualize file and registry write failures to per-user locations' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.2.1 ──────────────────────────────────────
function Apply-CIS_2_3_2_1 {
    <#
    .SYNOPSIS
        Ensure 'Audit: Force audit policy subcategory settings (Windows Vista or later) to override audit policy category settings' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.2.1" -Title "Ensure 'Audit: Force audit policy subcategory settings (Windows Vista or later) to override audit policy category settings' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa' -Name 'SCENoApplyLegacyAuditPolicy' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.2.1" -Title "Ensure 'Audit: Force audit policy subcategory settings (Windows Vista or later) to override audit policy category settings' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.2.1" -Title "Ensure 'Audit: Force audit policy subcategory settings (Windows Vista or later) to override audit policy category settings' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.2.2 ──────────────────────────────────────
function Apply-CIS_2_3_2_2 {
    <#
    .SYNOPSIS
        Ensure 'Audit: Shut down system immediately if unable to log security audits' is set to 'Disabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.2.2" -Title "Ensure 'Audit: Shut down system immediately if unable to log security audits' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Lsa' -Name 'CrashOnAuditFail' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.2.2" -Title "Ensure 'Audit: Shut down system immediately if unable to log security audits' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.2.2" -Title "Ensure 'Audit: Shut down system immediately if unable to log security audits' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.4.1 ──────────────────────────────────────
function Apply-CIS_2_3_4_1 {
    <#
    .SYNOPSIS
        Ensure 'Devices: Prevent users from installing printer drivers' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.4.1" -Title "Ensure 'Devices: Prevent users from installing printer drivers' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Print\Providers\LanMan Print Services\Servers' -Name 'AddPrinterDrivers' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.4.1" -Title "Ensure 'Devices: Prevent users from installing printer drivers' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.4.1" -Title "Ensure 'Devices: Prevent users from installing printer drivers' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.7.1 ──────────────────────────────────────
function Apply-CIS_2_3_7_1 {
    <#
    .SYNOPSIS
        Ensure 'Interactive logon: Do not require CTRL+ALT+DEL' is set to 'Disabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.7.1" -Title "Ensure 'Interactive logon: Do not require CTRL+ALT+DEL' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System' -Name 'DisableCAD' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.7.1" -Title "Ensure 'Interactive logon: Do not require CTRL+ALT+DEL' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.7.1" -Title "Ensure 'Interactive logon: Do not require CTRL+ALT+DEL' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.7.2 ──────────────────────────────────────
function Apply-CIS_2_3_7_2 {
    <#
    .SYNOPSIS
        Ensure 'Interactive logon: Don't display last signed-in' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.7.2" -Title "Ensure 'Interactive logon: Don't display last signed-in' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System' -Name 'DontDisplayLastUserName' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.7.2" -Title "Ensure 'Interactive logon: Don't display last signed-in' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.7.2" -Title "Ensure 'Interactive logon: Don't display last signed-in' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.7.3 ──────────────────────────────────────
function Apply-CIS_2_3_7_3 {
    <#
    .SYNOPSIS
        Ensure 'Interactive logon: Machine account lockout threshold' is set to '10 or fewer invalid logon attempts, but not 0'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.7.3" -Title "Ensure 'Interactive logon: Machine account lockout threshold' is set to '10 or fewer invalid logon attempts, but not 0'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System' -Name 'MaxDevicePasswordFailedAttempts' -Value 10 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.7.3" -Title "Ensure 'Interactive logon: Machine account lockout threshold' is set to '10 or fewer invalid logon attempts, but not 0'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.7.3" -Title "Ensure 'Interactive logon: Machine account lockout threshold' is set to '10 or fewer invalid logon attempts, but not 0'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.7.4 ──────────────────────────────────────
function Apply-CIS_2_3_7_4 {
    <#
    .SYNOPSIS
        Ensure 'Interactive logon: Machine inactivity limit' is set to '900 or fewer second(s), but not 0'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.7.4" -Title "Ensure 'Interactive logon: Machine inactivity limit' is set to '900 or fewer second(s), but not 0'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System' -Name 'InactivityTimeoutSecs' -Value 900 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.7.4" -Title "Ensure 'Interactive logon: Machine inactivity limit' is set to '900 or fewer second(s), but not 0'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.7.4" -Title "Ensure 'Interactive logon: Machine inactivity limit' is set to '900 or fewer second(s), but not 0'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.7.5 ──────────────────────────────────────
function Apply-CIS_2_3_7_5 {
    <#
    .SYNOPSIS
        Configure 'Interactive logon: Message text for users attempting to log on'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.7.5" -Title "Configure 'Interactive logon: Message text for users attempting to log on'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System' -Name 'LegalNoticeText' -Value 'Authorized users only. All activity is monitored.' -Type String -Force
        Write-RuleResult -RuleId "2.3.7.5" -Title "Configure 'Interactive logon: Message text for users attempting to log on'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.7.5" -Title "Configure 'Interactive logon: Message text for users attempting to log on'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.7.6 ──────────────────────────────────────
function Apply-CIS_2_3_7_6 {
    <#
    .SYNOPSIS
        Configure 'Interactive logon: Message title for users attempting to log on'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.7.6" -Title "Configure 'Interactive logon: Message title for users attempting to log on'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System' -Name 'LegalNoticeCaption' -Value 'Warning' -Type String -Force
        Write-RuleResult -RuleId "2.3.7.6" -Title "Configure 'Interactive logon: Message title for users attempting to log on'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.7.6" -Title "Configure 'Interactive logon: Message title for users attempting to log on'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.7.7 ──────────────────────────────────────
function Apply-CIS_2_3_7_7 {
    <#
    .SYNOPSIS
        Ensure 'Interactive logon: Prompt user to change password before expiration' is set to 'between 5 and 14 days'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.7.7" -Title "Ensure 'Interactive logon: Prompt user to change password before expiration' is set to 'between 5 and 14 days'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System' -Name 'PasswordExpiryWarning' -Value 14 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.7.7" -Title "Ensure 'Interactive logon: Prompt user to change password before expiration' is set to 'between 5 and 14 days'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.7.7" -Title "Ensure 'Interactive logon: Prompt user to change password before expiration' is set to 'between 5 and 14 days'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.7.8 ──────────────────────────────────────
function Apply-CIS_2_3_7_8 {
    <#
    .SYNOPSIS
        Ensure 'Interactive logon: Smart card removal behavior' is set to 'Lock Workstation' or higher
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.7.8" -Title "Ensure 'Interactive logon: Smart card removal behavior' is set to 'Lock Workstation' or higher" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System' -Name 'ScRemoveOption' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.7.8" -Title "Ensure 'Interactive logon: Smart card removal behavior' is set to 'Lock Workstation' or higher" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.7.8" -Title "Ensure 'Interactive logon: Smart card removal behavior' is set to 'Lock Workstation' or higher" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.8.1 ──────────────────────────────────────
function Apply-CIS_2_3_8_1 {
    <#
    .SYNOPSIS
        Ensure 'Microsoft network client: Digitally sign communications (always)' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.8.1" -Title "Ensure 'Microsoft network client: Digitally sign communications (always)' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\LanmanWorkstation\Parameters' -Name 'RequireSecuritySignature' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.8.1" -Title "Ensure 'Microsoft network client: Digitally sign communications (always)' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.8.1" -Title "Ensure 'Microsoft network client: Digitally sign communications (always)' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.8.2 ──────────────────────────────────────
function Apply-CIS_2_3_8_2 {
    <#
    .SYNOPSIS
        Ensure 'Microsoft network client: Digitally sign communications (if server agrees)' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.8.2" -Title "Ensure 'Microsoft network client: Digitally sign communications (if server agrees)' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\LanmanWorkstation\Parameters' -Name 'EnableSecuritySignature' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.8.2" -Title "Ensure 'Microsoft network client: Digitally sign communications (if server agrees)' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.8.2" -Title "Ensure 'Microsoft network client: Digitally sign communications (if server agrees)' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.8.3 ──────────────────────────────────────
function Apply-CIS_2_3_8_3 {
    <#
    .SYNOPSIS
        Ensure 'Microsoft network client: Send unencrypted password to third-party SMB servers' is set to 'Disabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.8.3" -Title "Ensure 'Microsoft network client: Send unencrypted password to third-party SMB servers' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\LanmanWorkstation\Parameters' -Name 'EnablePlainTextPassword' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.8.3" -Title "Ensure 'Microsoft network client: Send unencrypted password to third-party SMB servers' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.8.3" -Title "Ensure 'Microsoft network client: Send unencrypted password to third-party SMB servers' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.9.1 ──────────────────────────────────────
function Apply-CIS_2_3_9_1 {
    <#
    .SYNOPSIS
        Ensure 'Microsoft network server: Amount of idle time required before suspending session' is set to '15 or fewer minute(s)'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.9.1" -Title "Ensure 'Microsoft network server: Amount of idle time required before suspending session' is set to '15 or fewer minute(s)'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\LanManServer\Parameters' -Name 'autodisconnect' -Value 15 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.9.1" -Title "Ensure 'Microsoft network server: Amount of idle time required before suspending session' is set to '15 or fewer minute(s)'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.9.1" -Title "Ensure 'Microsoft network server: Amount of idle time required before suspending session' is set to '15 or fewer minute(s)'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.9.2 ──────────────────────────────────────
function Apply-CIS_2_3_9_2 {
    <#
    .SYNOPSIS
        Ensure 'Microsoft network server: Digitally sign communications (always)' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.9.2" -Title "Ensure 'Microsoft network server: Digitally sign communications (always)' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\LanManServer\Parameters' -Name 'RequireSecuritySignature' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.9.2" -Title "Ensure 'Microsoft network server: Digitally sign communications (always)' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.9.2" -Title "Ensure 'Microsoft network server: Digitally sign communications (always)' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.9.3 ──────────────────────────────────────
function Apply-CIS_2_3_9_3 {
    <#
    .SYNOPSIS
        Ensure 'Microsoft network server: Digitally sign communications (if client agrees)' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.9.3" -Title "Ensure 'Microsoft network server: Digitally sign communications (if client agrees)' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\LanManServer\Parameters' -Name 'EnableSecuritySignature' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.9.3" -Title "Ensure 'Microsoft network server: Digitally sign communications (if client agrees)' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.9.3" -Title "Ensure 'Microsoft network server: Digitally sign communications (if client agrees)' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.9.4 ──────────────────────────────────────
function Apply-CIS_2_3_9_4 {
    <#
    .SYNOPSIS
        Ensure 'Microsoft network server: Disconnect clients when logon hours expire' is set to 'Enabled'
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.9.4" -Title "Ensure 'Microsoft network server: Disconnect clients when logon hours expire' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\LanManServer\Parameters' -Name 'enableforcedlogoff' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.9.4" -Title "Ensure 'Microsoft network server: Disconnect clients when logon hours expire' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.9.4" -Title "Ensure 'Microsoft network server: Disconnect clients when logon hours expire' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 2.3.9.5 ──────────────────────────────────────
function Apply-CIS_2_3_9_5 {
    <#
    .SYNOPSIS
        Ensure 'Microsoft network server: Server SPN target name validation level' is set to 'Accept if provided by client' or higher
    .NOTES
        Category:       Local Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "2.3.9.5" -Title "Ensure 'Microsoft network server: Server SPN target name validation level' is set to 'Accept if provided by client' or higher" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\LanManServer\Parameters' -Name 'SmbServerNameHardeningLevel' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "2.3.9.5" -Title "Ensure 'Microsoft network server: Server SPN target name validation level' is set to 'Accept if provided by client' or higher" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "2.3.9.5" -Title "Ensure 'Microsoft network server: Server SPN target name validation level' is set to 'Accept if provided by client' or higher" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.1.1 ──────────────────────────────────────
function Apply-CIS_17_1_1 {
    <#
    .SYNOPSIS
        Ensure 'Audit Credential Validation' is set to 'Success and Failure'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.1.1" -Title "Ensure 'Audit Credential Validation' is set to 'Success and Failure'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Credential Validation" /success:enable /failure:enable
        Write-RuleResult -RuleId "17.1.1" -Title "Ensure 'Audit Credential Validation' is set to 'Success and Failure'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.1.1" -Title "Ensure 'Audit Credential Validation' is set to 'Success and Failure'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.2.1 ──────────────────────────────────────
function Apply-CIS_17_2_1 {
    <#
    .SYNOPSIS
        Ensure 'Audit Application Group Management' is set to 'Success and Failure'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.2.1" -Title "Ensure 'Audit Application Group Management' is set to 'Success and Failure'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Application Group Management" /success:enable /failure:enable
        Write-RuleResult -RuleId "17.2.1" -Title "Ensure 'Audit Application Group Management' is set to 'Success and Failure'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.2.1" -Title "Ensure 'Audit Application Group Management' is set to 'Success and Failure'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.2.2 ──────────────────────────────────────
function Apply-CIS_17_2_2 {
    <#
    .SYNOPSIS
        Ensure 'Audit Security Group Management' is set to include 'Success'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.2.2" -Title "Ensure 'Audit Security Group Management' is set to include 'Success'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Security Group Management" /success:enable /failure:disable
        Write-RuleResult -RuleId "17.2.2" -Title "Ensure 'Audit Security Group Management' is set to include 'Success'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.2.2" -Title "Ensure 'Audit Security Group Management' is set to include 'Success'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.2.3 ──────────────────────────────────────
function Apply-CIS_17_2_3 {
    <#
    .SYNOPSIS
        Ensure 'Audit User Account Management' is set to 'Success and Failure'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.2.3" -Title "Ensure 'Audit User Account Management' is set to 'Success and Failure'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"User Account Management" /success:enable /failure:enable
        Write-RuleResult -RuleId "17.2.3" -Title "Ensure 'Audit User Account Management' is set to 'Success and Failure'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.2.3" -Title "Ensure 'Audit User Account Management' is set to 'Success and Failure'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.3.1 ──────────────────────────────────────
function Apply-CIS_17_3_1 {
    <#
    .SYNOPSIS
        Ensure 'Audit PNP Activity' is set to include 'Success'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.3.1" -Title "Ensure 'Audit PNP Activity' is set to include 'Success'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"PNP Activity" /success:enable /failure:disable
        Write-RuleResult -RuleId "17.3.1" -Title "Ensure 'Audit PNP Activity' is set to include 'Success'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.3.1" -Title "Ensure 'Audit PNP Activity' is set to include 'Success'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.3.2 ──────────────────────────────────────
function Apply-CIS_17_3_2 {
    <#
    .SYNOPSIS
        Ensure 'Audit Process Creation' is set to include 'Success'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.3.2" -Title "Ensure 'Audit Process Creation' is set to include 'Success'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Process Creation" /success:enable /failure:disable
        Write-RuleResult -RuleId "17.3.2" -Title "Ensure 'Audit Process Creation' is set to include 'Success'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.3.2" -Title "Ensure 'Audit Process Creation' is set to include 'Success'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.5.1 ──────────────────────────────────────
function Apply-CIS_17_5_1 {
    <#
    .SYNOPSIS
        Ensure 'Audit Account Lockout' is set to include 'Failure'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.5.1" -Title "Ensure 'Audit Account Lockout' is set to include 'Failure'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Account Lockout" /success:disable /failure:enable
        Write-RuleResult -RuleId "17.5.1" -Title "Ensure 'Audit Account Lockout' is set to include 'Failure'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.5.1" -Title "Ensure 'Audit Account Lockout' is set to include 'Failure'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.5.2 ──────────────────────────────────────
function Apply-CIS_17_5_2 {
    <#
    .SYNOPSIS
        Ensure 'Audit Group Membership' is set to include 'Success'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.5.2" -Title "Ensure 'Audit Group Membership' is set to include 'Success'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Group Membership" /success:enable /failure:disable
        Write-RuleResult -RuleId "17.5.2" -Title "Ensure 'Audit Group Membership' is set to include 'Success'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.5.2" -Title "Ensure 'Audit Group Membership' is set to include 'Success'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.5.3 ──────────────────────────────────────
function Apply-CIS_17_5_3 {
    <#
    .SYNOPSIS
        Ensure 'Audit Logoff' is set to include 'Success'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.5.3" -Title "Ensure 'Audit Logoff' is set to include 'Success'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Logoff" /success:enable /failure:disable
        Write-RuleResult -RuleId "17.5.3" -Title "Ensure 'Audit Logoff' is set to include 'Success'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.5.3" -Title "Ensure 'Audit Logoff' is set to include 'Success'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.5.4 ──────────────────────────────────────
function Apply-CIS_17_5_4 {
    <#
    .SYNOPSIS
        Ensure 'Audit Logon' is set to 'Success and Failure'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.5.4" -Title "Ensure 'Audit Logon' is set to 'Success and Failure'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Logon" /success:enable /failure:enable
        Write-RuleResult -RuleId "17.5.4" -Title "Ensure 'Audit Logon' is set to 'Success and Failure'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.5.4" -Title "Ensure 'Audit Logon' is set to 'Success and Failure'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.5.5 ──────────────────────────────────────
function Apply-CIS_17_5_5 {
    <#
    .SYNOPSIS
        Ensure 'Audit Other Logon/Logoff Events' is set to 'Success and Failure'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.5.5" -Title "Ensure 'Audit Other Logon/Logoff Events' is set to 'Success and Failure'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Other Logon/Logoff Events" /success:enable /failure:enable
        Write-RuleResult -RuleId "17.5.5" -Title "Ensure 'Audit Other Logon/Logoff Events' is set to 'Success and Failure'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.5.5" -Title "Ensure 'Audit Other Logon/Logoff Events' is set to 'Success and Failure'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.5.6 ──────────────────────────────────────
function Apply-CIS_17_5_6 {
    <#
    .SYNOPSIS
        Ensure 'Audit Special Logon' is set to include 'Success'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.5.6" -Title "Ensure 'Audit Special Logon' is set to include 'Success'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Special Logon" /success:enable /failure:disable
        Write-RuleResult -RuleId "17.5.6" -Title "Ensure 'Audit Special Logon' is set to include 'Success'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.5.6" -Title "Ensure 'Audit Special Logon' is set to include 'Success'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.6.1 ──────────────────────────────────────
function Apply-CIS_17_6_1 {
    <#
    .SYNOPSIS
        Ensure 'Audit Detailed File Share' is set to include 'Failure'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.6.1" -Title "Ensure 'Audit Detailed File Share' is set to include 'Failure'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Detailed File Share" /success:disable /failure:enable
        Write-RuleResult -RuleId "17.6.1" -Title "Ensure 'Audit Detailed File Share' is set to include 'Failure'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.6.1" -Title "Ensure 'Audit Detailed File Share' is set to include 'Failure'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.6.2 ──────────────────────────────────────
function Apply-CIS_17_6_2 {
    <#
    .SYNOPSIS
        Ensure 'Audit File Share' is set to 'Success and Failure'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.6.2" -Title "Ensure 'Audit File Share' is set to 'Success and Failure'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"File Share" /success:enable /failure:enable
        Write-RuleResult -RuleId "17.6.2" -Title "Ensure 'Audit File Share' is set to 'Success and Failure'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.6.2" -Title "Ensure 'Audit File Share' is set to 'Success and Failure'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.6.3 ──────────────────────────────────────
function Apply-CIS_17_6_3 {
    <#
    .SYNOPSIS
        Ensure 'Audit Other Object Access Events' is set to 'Success and Failure'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.6.3" -Title "Ensure 'Audit Other Object Access Events' is set to 'Success and Failure'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Other Object Access Events" /success:enable /failure:enable
        Write-RuleResult -RuleId "17.6.3" -Title "Ensure 'Audit Other Object Access Events' is set to 'Success and Failure'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.6.3" -Title "Ensure 'Audit Other Object Access Events' is set to 'Success and Failure'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.6.4 ──────────────────────────────────────
function Apply-CIS_17_6_4 {
    <#
    .SYNOPSIS
        Ensure 'Audit Removable Storage' is set to 'Success and Failure'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.6.4" -Title "Ensure 'Audit Removable Storage' is set to 'Success and Failure'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Removable Storage" /success:enable /failure:enable
        Write-RuleResult -RuleId "17.6.4" -Title "Ensure 'Audit Removable Storage' is set to 'Success and Failure'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.6.4" -Title "Ensure 'Audit Removable Storage' is set to 'Success and Failure'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.7.1 ──────────────────────────────────────
function Apply-CIS_17_7_1 {
    <#
    .SYNOPSIS
        Ensure 'Audit Audit Policy Change' is set to include 'Success'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.7.1" -Title "Ensure 'Audit Audit Policy Change' is set to include 'Success'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Audit Policy Change" /success:enable /failure:disable
        Write-RuleResult -RuleId "17.7.1" -Title "Ensure 'Audit Audit Policy Change' is set to include 'Success'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.7.1" -Title "Ensure 'Audit Audit Policy Change' is set to include 'Success'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.7.2 ──────────────────────────────────────
function Apply-CIS_17_7_2 {
    <#
    .SYNOPSIS
        Ensure 'Audit Authentication Policy Change' is set to include 'Success'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.7.2" -Title "Ensure 'Audit Authentication Policy Change' is set to include 'Success'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Authentication Policy Change" /success:enable /failure:disable
        Write-RuleResult -RuleId "17.7.2" -Title "Ensure 'Audit Authentication Policy Change' is set to include 'Success'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.7.2" -Title "Ensure 'Audit Authentication Policy Change' is set to include 'Success'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.7.3 ──────────────────────────────────────
function Apply-CIS_17_7_3 {
    <#
    .SYNOPSIS
        Ensure 'Audit Authorization Policy Change' is set to include 'Success'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.7.3" -Title "Ensure 'Audit Authorization Policy Change' is set to include 'Success'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Authorization Policy Change" /success:enable /failure:disable
        Write-RuleResult -RuleId "17.7.3" -Title "Ensure 'Audit Authorization Policy Change' is set to include 'Success'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.7.3" -Title "Ensure 'Audit Authorization Policy Change' is set to include 'Success'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.7.4 ──────────────────────────────────────
function Apply-CIS_17_7_4 {
    <#
    .SYNOPSIS
        Ensure 'Audit MPSSVC Rule-Level Policy Change' is set to 'Success and Failure'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.7.4" -Title "Ensure 'Audit MPSSVC Rule-Level Policy Change' is set to 'Success and Failure'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"MPSSVC Rule-Level Policy Change" /success:enable /failure:enable
        Write-RuleResult -RuleId "17.7.4" -Title "Ensure 'Audit MPSSVC Rule-Level Policy Change' is set to 'Success and Failure'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.7.4" -Title "Ensure 'Audit MPSSVC Rule-Level Policy Change' is set to 'Success and Failure'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.7.5 ──────────────────────────────────────
function Apply-CIS_17_7_5 {
    <#
    .SYNOPSIS
        Ensure 'Audit Other Policy Change Events' is set to include 'Failure'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.7.5" -Title "Ensure 'Audit Other Policy Change Events' is set to include 'Failure'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Other Policy Change Events" /success:disable /failure:enable
        Write-RuleResult -RuleId "17.7.5" -Title "Ensure 'Audit Other Policy Change Events' is set to include 'Failure'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.7.5" -Title "Ensure 'Audit Other Policy Change Events' is set to include 'Failure'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.8.1 ──────────────────────────────────────
function Apply-CIS_17_8_1 {
    <#
    .SYNOPSIS
        Ensure 'Audit Sensitive Privilege Use' is set to 'Success and Failure'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.8.1" -Title "Ensure 'Audit Sensitive Privilege Use' is set to 'Success and Failure'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Sensitive Privilege Use" /success:enable /failure:enable
        Write-RuleResult -RuleId "17.8.1" -Title "Ensure 'Audit Sensitive Privilege Use' is set to 'Success and Failure'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.8.1" -Title "Ensure 'Audit Sensitive Privilege Use' is set to 'Success and Failure'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.9.1 ──────────────────────────────────────
function Apply-CIS_17_9_1 {
    <#
    .SYNOPSIS
        Ensure 'Audit IPsec Driver' is set to 'Success and Failure'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.9.1" -Title "Ensure 'Audit IPsec Driver' is set to 'Success and Failure'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"IPsec Driver" /success:enable /failure:enable
        Write-RuleResult -RuleId "17.9.1" -Title "Ensure 'Audit IPsec Driver' is set to 'Success and Failure'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.9.1" -Title "Ensure 'Audit IPsec Driver' is set to 'Success and Failure'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.9.2 ──────────────────────────────────────
function Apply-CIS_17_9_2 {
    <#
    .SYNOPSIS
        Ensure 'Audit Other System Events' is set to 'Success and Failure'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.9.2" -Title "Ensure 'Audit Other System Events' is set to 'Success and Failure'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Other System Events" /success:enable /failure:enable
        Write-RuleResult -RuleId "17.9.2" -Title "Ensure 'Audit Other System Events' is set to 'Success and Failure'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.9.2" -Title "Ensure 'Audit Other System Events' is set to 'Success and Failure'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.9.3 ──────────────────────────────────────
function Apply-CIS_17_9_3 {
    <#
    .SYNOPSIS
        Ensure 'Audit Security State Change' is set to include 'Success'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.9.3" -Title "Ensure 'Audit Security State Change' is set to include 'Success'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Security State Change" /success:enable /failure:disable
        Write-RuleResult -RuleId "17.9.3" -Title "Ensure 'Audit Security State Change' is set to include 'Success'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.9.3" -Title "Ensure 'Audit Security State Change' is set to include 'Success'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.9.4 ──────────────────────────────────────
function Apply-CIS_17_9_4 {
    <#
    .SYNOPSIS
        Ensure 'Audit Security System Extension' is set to include 'Success'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.9.4" -Title "Ensure 'Audit Security System Extension' is set to include 'Success'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"Security System Extension" /success:enable /failure:disable
        Write-RuleResult -RuleId "17.9.4" -Title "Ensure 'Audit Security System Extension' is set to include 'Success'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.9.4" -Title "Ensure 'Audit Security System Extension' is set to include 'Success'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 17.9.5 ──────────────────────────────────────
function Apply-CIS_17_9_5 {
    <#
    .SYNOPSIS
        Ensure 'Audit System Integrity' is set to 'Success and Failure'
    .NOTES
        Category:       Advanced Audit Policy
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "17.9.5" -Title "Ensure 'Audit System Integrity' is set to 'Success and Failure'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        auditpol /set /subcategory:"System Integrity" /success:enable /failure:enable
        Write-RuleResult -RuleId "17.9.5" -Title "Ensure 'Audit System Integrity' is set to 'Success and Failure'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "17.9.5" -Title "Ensure 'Audit System Integrity' is set to 'Success and Failure'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.1.1.1 ──────────────────────────────────────
function Apply-CIS_18_1_1_1 {
    <#
    .SYNOPSIS
        Ensure 'Prevent enabling lock screen camera' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.1.1.1" -Title "Ensure 'Prevent enabling lock screen camera' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Personalization')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Personalization' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Personalization' -Name 'NoLockScreenCamera' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.1.1.1" -Title "Ensure 'Prevent enabling lock screen camera' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.1.1.1" -Title "Ensure 'Prevent enabling lock screen camera' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.1.1.2 ──────────────────────────────────────
function Apply-CIS_18_1_1_2 {
    <#
    .SYNOPSIS
        Ensure 'Prevent enabling lock screen slide show' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.1.1.2" -Title "Ensure 'Prevent enabling lock screen slide show' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Personalization')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Personalization' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Personalization' -Name 'NoLockScreenSlideshow' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.1.1.2" -Title "Ensure 'Prevent enabling lock screen slide show' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.1.1.2" -Title "Ensure 'Prevent enabling lock screen slide show' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.1.2.2 ──────────────────────────────────────
function Apply-CIS_18_1_2_2 {
    <#
    .SYNOPSIS
        Ensure 'Allow users to enable online speech recognition services' is set to 'Disabled'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.1.2.2" -Title "Ensure 'Allow users to enable online speech recognition services' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\InputPersonalization')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\InputPersonalization' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\InputPersonalization' -Name 'AllowInputPersonalization' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.1.2.2" -Title "Ensure 'Allow users to enable online speech recognition services' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.1.2.2" -Title "Ensure 'Allow users to enable online speech recognition services' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.1.3 ──────────────────────────────────────
function Apply-CIS_18_1_3 {
    <#
    .SYNOPSIS
        Ensure 'Allow Online Tips' is set to 'Disabled'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.1.3" -Title "Ensure 'Allow Online Tips' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Explorer')) { New-Item -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Explorer' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Explorer' -Name 'AllowOnlineTips' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.1.3" -Title "Ensure 'Allow Online Tips' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.1.3" -Title "Ensure 'Allow Online Tips' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.4.1 ──────────────────────────────────────
function Apply-CIS_18_4_1 {
    <#
    .SYNOPSIS
        Ensure 'Configure SMB v1 client driver' is set to 'Enabled: Disable driver (recommended)'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.4.1" -Title "Ensure 'Configure SMB v1 client driver' is set to 'Enabled: Disable driver (recommended)'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Services\mrxsmb10')) { New-Item -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\mrxsmb10' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\mrxsmb10' -Name 'Start' -Value 4 -Type DWord -Force
        Write-RuleResult -RuleId "18.4.1" -Title "Ensure 'Configure SMB v1 client driver' is set to 'Enabled: Disable driver (recommended)'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.4.1" -Title "Ensure 'Configure SMB v1 client driver' is set to 'Enabled: Disable driver (recommended)'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.4.2 ──────────────────────────────────────
function Apply-CIS_18_4_2 {
    <#
    .SYNOPSIS
        Ensure 'Configure SMB v1 server' is set to 'Disabled'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.4.2" -Title "Ensure 'Configure SMB v1 server' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Services\LanmanServer\Parameters')) { New-Item -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\LanmanServer\Parameters' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\LanmanServer\Parameters' -Name 'SMB1' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.4.2" -Title "Ensure 'Configure SMB v1 server' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.4.2" -Title "Ensure 'Configure SMB v1 server' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.4.3 ──────────────────────────────────────
function Apply-CIS_18_4_3 {
    <#
    .SYNOPSIS
        Ensure 'Enable Certificate Padding' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.4.3" -Title "Ensure 'Enable Certificate Padding' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Cryptography\Configuration\SSL\00010002')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Cryptography\Configuration\SSL\00010002' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Cryptography\Configuration\SSL\00010002' -Name 'EnableCertPadding' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.4.3" -Title "Ensure 'Enable Certificate Padding' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.4.3" -Title "Ensure 'Enable Certificate Padding' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.4.4 ──────────────────────────────────────
function Apply-CIS_18_4_4 {
    <#
    .SYNOPSIS
        Ensure 'Enable Structured Exception Handling Overwrite Protection (SEHOP)' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.4.4" -Title "Ensure 'Enable Structured Exception Handling Overwrite Protection (SEHOP)' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Control\Session Manager\kernel')) { New-Item -Path 'HKLM:\SYSTEM\CurrentControlSet\Control\Session Manager\kernel' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Control\Session Manager\kernel' -Name 'DisableExceptionChainValidation' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.4.4" -Title "Ensure 'Enable Structured Exception Handling Overwrite Protection (SEHOP)' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.4.4" -Title "Ensure 'Enable Structured Exception Handling Overwrite Protection (SEHOP)' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.4.5 ──────────────────────────────────────
function Apply-CIS_18_4_5 {
    <#
    .SYNOPSIS
        Ensure 'NetBT NodeType configuration' is set to 'Enabled: P-node (recommended)'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.4.5" -Title "Ensure 'NetBT NodeType configuration' is set to 'Enabled: P-node (recommended)'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Services\NetBT\Parameters')) { New-Item -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\NetBT\Parameters' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\NetBT\Parameters' -Name 'NodeType' -Value 2 -Type DWord -Force
        Write-RuleResult -RuleId "18.4.5" -Title "Ensure 'NetBT NodeType configuration' is set to 'Enabled: P-node (recommended)'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.4.5" -Title "Ensure 'NetBT NodeType configuration' is set to 'Enabled: P-node (recommended)'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.4.6 ──────────────────────────────────────
function Apply-CIS_18_4_6 {
    <#
    .SYNOPSIS
        Ensure 'WDigest Authentication' is set to 'Disabled'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.4.6" -Title "Ensure 'WDigest Authentication' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Control\SecurityProviders\WDigest')) { New-Item -Path 'HKLM:\SYSTEM\CurrentControlSet\Control\SecurityProviders\WDigest' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Control\SecurityProviders\WDigest' -Name 'UseLogonCredential' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.4.6" -Title "Ensure 'WDigest Authentication' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.4.6" -Title "Ensure 'WDigest Authentication' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.5.1 ──────────────────────────────────────
function Apply-CIS_18_5_1 {
    <#
    .SYNOPSIS
        Ensure 'MSS: (AutoAdminLogon) Enable Automatic Logon' is set to 'Disabled'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.5.1" -Title "Ensure 'MSS: (AutoAdminLogon) Enable Automatic Logon' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon')) { New-Item -Path 'HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon' -Name 'AutoAdminLogon' -Value '0' -Type String -Force
        Write-RuleResult -RuleId "18.5.1" -Title "Ensure 'MSS: (AutoAdminLogon) Enable Automatic Logon' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.5.1" -Title "Ensure 'MSS: (AutoAdminLogon) Enable Automatic Logon' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.5.10 ──────────────────────────────────────
function Apply-CIS_18_5_10 {
    <#
    .SYNOPSIS
        Ensure 'MSS: (ScreenSaverGracePeriod) The time in seconds...' is set to 'Enabled: 5 or fewer seconds'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.5.10" -Title "Ensure 'MSS: (ScreenSaverGracePeriod) The time in seconds...' is set to 'Enabled: 5 or fewer seconds'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon')) { New-Item -Path 'HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon' -Name 'ScreenSaverGracePeriod' -Value '5' -Type String -Force
        Write-RuleResult -RuleId "18.5.10" -Title "Ensure 'MSS: (ScreenSaverGracePeriod) The time in seconds...' is set to 'Enabled: 5 or fewer seconds'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.5.10" -Title "Ensure 'MSS: (ScreenSaverGracePeriod) The time in seconds...' is set to 'Enabled: 5 or fewer seconds'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.5.11 ──────────────────────────────────────
function Apply-CIS_18_5_11 {
    <#
    .SYNOPSIS
        Ensure 'MSS: (TcpMaxDataRetransmissions IPv6) How many times unacknowledged data' is set to 'Enabled: 3'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.5.11" -Title "Ensure 'MSS: (TcpMaxDataRetransmissions IPv6) How many times unacknowledged data' is set to 'Enabled: 3'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip6\Parameters')) { New-Item -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip6\Parameters' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip6\Parameters' -Name 'TcpMaxDataRetransmissions' -Value 3 -Type DWord -Force
        Write-RuleResult -RuleId "18.5.11" -Title "Ensure 'MSS: (TcpMaxDataRetransmissions IPv6) How many times unacknowledged data' is set to 'Enabled: 3'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.5.11" -Title "Ensure 'MSS: (TcpMaxDataRetransmissions IPv6) How many times unacknowledged data' is set to 'Enabled: 3'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.5.12 ──────────────────────────────────────
function Apply-CIS_18_5_12 {
    <#
    .SYNOPSIS
        Ensure 'MSS: (TcpMaxDataRetransmissions) How many times unacknowledged data' is set to 'Enabled: 3'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.5.12" -Title "Ensure 'MSS: (TcpMaxDataRetransmissions) How many times unacknowledged data' is set to 'Enabled: 3'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters')) { New-Item -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters' -Name 'TcpMaxDataRetransmissions' -Value 3 -Type DWord -Force
        Write-RuleResult -RuleId "18.5.12" -Title "Ensure 'MSS: (TcpMaxDataRetransmissions) How many times unacknowledged data' is set to 'Enabled: 3'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.5.12" -Title "Ensure 'MSS: (TcpMaxDataRetransmissions) How many times unacknowledged data' is set to 'Enabled: 3'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.5.13 ──────────────────────────────────────
function Apply-CIS_18_5_13 {
    <#
    .SYNOPSIS
        Ensure 'MSS: (WarningLevel) Percentage threshold for the security event log' is set to 'Enabled: 90% or less'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.5.13" -Title "Ensure 'MSS: (WarningLevel) Percentage threshold for the security event log' is set to 'Enabled: 90% or less'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Eventlog\Security')) { New-Item -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Eventlog\Security' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Eventlog\Security' -Name 'WarningLevel' -Value 90 -Type DWord -Force
        Write-RuleResult -RuleId "18.5.13" -Title "Ensure 'MSS: (WarningLevel) Percentage threshold for the security event log' is set to 'Enabled: 90% or less'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.5.13" -Title "Ensure 'MSS: (WarningLevel) Percentage threshold for the security event log' is set to 'Enabled: 90% or less'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.5.2 ──────────────────────────────────────
function Apply-CIS_18_5_2 {
    <#
    .SYNOPSIS
        Ensure 'MSS: (DisableIPSourceRouting IPv6) IP source routing protection level' is set to 'Enabled: Highest completely disabled'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.5.2" -Title "Ensure 'MSS: (DisableIPSourceRouting IPv6) IP source routing protection level' is set to 'Enabled: Highest completely disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip6\Parameters')) { New-Item -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip6\Parameters' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip6\Parameters' -Name 'DisableIPSourceRouting' -Value 2 -Type DWord -Force
        Write-RuleResult -RuleId "18.5.2" -Title "Ensure 'MSS: (DisableIPSourceRouting IPv6) IP source routing protection level' is set to 'Enabled: Highest completely disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.5.2" -Title "Ensure 'MSS: (DisableIPSourceRouting IPv6) IP source routing protection level' is set to 'Enabled: Highest completely disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.5.3 ──────────────────────────────────────
function Apply-CIS_18_5_3 {
    <#
    .SYNOPSIS
        Ensure 'MSS: (DisableIPSourceRouting) IP source routing protection level' is set to 'Enabled: Highest completely disabled'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.5.3" -Title "Ensure 'MSS: (DisableIPSourceRouting) IP source routing protection level' is set to 'Enabled: Highest completely disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters')) { New-Item -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters' -Name 'DisableIPSourceRouting' -Value 2 -Type DWord -Force
        Write-RuleResult -RuleId "18.5.3" -Title "Ensure 'MSS: (DisableIPSourceRouting) IP source routing protection level' is set to 'Enabled: Highest completely disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.5.3" -Title "Ensure 'MSS: (DisableIPSourceRouting) IP source routing protection level' is set to 'Enabled: Highest completely disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.5.4 ──────────────────────────────────────
function Apply-CIS_18_5_4 {
    <#
    .SYNOPSIS
        Ensure 'MSS: (DisableSavePassword) Prevent the dial-up password from being saved' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.5.4" -Title "Ensure 'MSS: (DisableSavePassword) Prevent the dial-up password from being saved' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Services\RasMan\Parameters')) { New-Item -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\RasMan\Parameters' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\RasMan\Parameters' -Name 'DisableSavePassword' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.5.4" -Title "Ensure 'MSS: (DisableSavePassword) Prevent the dial-up password from being saved' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.5.4" -Title "Ensure 'MSS: (DisableSavePassword) Prevent the dial-up password from being saved' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.5.5 ──────────────────────────────────────
function Apply-CIS_18_5_5 {
    <#
    .SYNOPSIS
        Ensure 'MSS: (EnableICMPRedirect) Allow ICMP redirects to override OSPF generated routes' is set to 'Disabled'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.5.5" -Title "Ensure 'MSS: (EnableICMPRedirect) Allow ICMP redirects to override OSPF generated routes' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters')) { New-Item -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters' -Name 'EnableICMPRedirect' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.5.5" -Title "Ensure 'MSS: (EnableICMPRedirect) Allow ICMP redirects to override OSPF generated routes' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.5.5" -Title "Ensure 'MSS: (EnableICMPRedirect) Allow ICMP redirects to override OSPF generated routes' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.5.6 ──────────────────────────────────────
function Apply-CIS_18_5_6 {
    <#
    .SYNOPSIS
        Ensure 'MSS: (KeepAliveTime) How often keep-alive packets are sent' is set to 'Enabled: 300,000'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.5.6" -Title "Ensure 'MSS: (KeepAliveTime) How often keep-alive packets are sent' is set to 'Enabled: 300,000'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters')) { New-Item -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters' -Name 'KeepAliveTime' -Value 300000 -Type DWord -Force
        Write-RuleResult -RuleId "18.5.6" -Title "Ensure 'MSS: (KeepAliveTime) How often keep-alive packets are sent' is set to 'Enabled: 300,000'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.5.6" -Title "Ensure 'MSS: (KeepAliveTime) How often keep-alive packets are sent' is set to 'Enabled: 300,000'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.5.7 ──────────────────────────────────────
function Apply-CIS_18_5_7 {
    <#
    .SYNOPSIS
        Ensure 'MSS: (NoNameReleaseOnDemand) Allow the computer to ignore NetBIOS name release requests...' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.5.7" -Title "Ensure 'MSS: (NoNameReleaseOnDemand) Allow the computer to ignore NetBIOS name release requests...' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Services\NetBT\Parameters')) { New-Item -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\NetBT\Parameters' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\NetBT\Parameters' -Name 'NoNameReleaseOnDemand' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.5.7" -Title "Ensure 'MSS: (NoNameReleaseOnDemand) Allow the computer to ignore NetBIOS name release requests...' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.5.7" -Title "Ensure 'MSS: (NoNameReleaseOnDemand) Allow the computer to ignore NetBIOS name release requests...' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.5.8 ──────────────────────────────────────
function Apply-CIS_18_5_8 {
    <#
    .SYNOPSIS
        Ensure 'MSS: (PerformRouterDiscovery) Allow IRDP to detect and configure Default Gateway' is set to 'Disabled'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.5.8" -Title "Ensure 'MSS: (PerformRouterDiscovery) Allow IRDP to detect and configure Default Gateway' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters')) { New-Item -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\Tcpip\Parameters' -Name 'PerformRouterDiscovery' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.5.8" -Title "Ensure 'MSS: (PerformRouterDiscovery) Allow IRDP to detect and configure Default Gateway' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.5.8" -Title "Ensure 'MSS: (PerformRouterDiscovery) Allow IRDP to detect and configure Default Gateway' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.5.9 ──────────────────────────────────────
function Apply-CIS_18_5_9 {
    <#
    .SYNOPSIS
        Ensure 'MSS: (SafeDllSearchMode) Enable Safe DLL search mode' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.5.9" -Title "Ensure 'MSS: (SafeDllSearchMode) Enable Safe DLL search mode' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Control\Session Manager')) { New-Item -Path 'HKLM:\SYSTEM\CurrentControlSet\Control\Session Manager' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Control\Session Manager' -Name 'SafeDllSearchMode' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.5.9" -Title "Ensure 'MSS: (SafeDllSearchMode) Enable Safe DLL search mode' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.5.9" -Title "Ensure 'MSS: (SafeDllSearchMode) Enable Safe DLL search mode' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.10.2 ──────────────────────────────────────
function Apply-CIS_18_6_10_2 {
    <#
    .SYNOPSIS
        Ensure 'Turn off Microsoft Peer-to-Peer Networking Services' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.10.2" -Title "Ensure 'Turn off Microsoft Peer-to-Peer Networking Services' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Peernet')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Peernet' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Peernet' -Name 'Disabled' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.10.2" -Title "Ensure 'Turn off Microsoft Peer-to-Peer Networking Services' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.10.2" -Title "Ensure 'Turn off Microsoft Peer-to-Peer Networking Services' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.11.2 ──────────────────────────────────────
function Apply-CIS_18_6_11_2 {
    <#
    .SYNOPSIS
        Ensure 'Prohibit installation and configuration of Network Bridge' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.11.2" -Title "Ensure 'Prohibit installation and configuration of Network Bridge' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Network Connections')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Network Connections' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Network Connections' -Name 'NC_AllowNetBridge_NLA' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.11.2" -Title "Ensure 'Prohibit installation and configuration of Network Bridge' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.11.2" -Title "Ensure 'Prohibit installation and configuration of Network Bridge' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.11.3 ──────────────────────────────────────
function Apply-CIS_18_6_11_3 {
    <#
    .SYNOPSIS
        Ensure 'Prohibit use of Internet Connection Sharing' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.11.3" -Title "Ensure 'Prohibit use of Internet Connection Sharing' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Network Connections')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Network Connections' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Network Connections' -Name 'NC_ShowSharedAccessUI' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.11.3" -Title "Ensure 'Prohibit use of Internet Connection Sharing' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.11.3" -Title "Ensure 'Prohibit use of Internet Connection Sharing' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.14.1 ──────────────────────────────────────
function Apply-CIS_18_6_14_1 {
    <#
    .SYNOPSIS
        Ensure 'Hardened UNC Paths' is set to 'Enabled' for NETLOGON and SYSVOL
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.14.1" -Title "Ensure 'Hardened UNC Paths' is set to 'Enabled' for NETLOGON and SYSVOL" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\NetworkProvider\HardenedPaths')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\NetworkProvider\HardenedPaths' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\NetworkProvider\HardenedPaths' -Name '\\*\NETLOGON' -Value 'RequireMutualAuthentication=1,RequireIntegrity=1,RequirePrivacy=1' -Type String -Force; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\NetworkProvider\HardenedPaths' -Name '\\*\SYSVOL' -Value 'RequireMutualAuthentication=1,RequireIntegrity=1,RequirePrivacy=1' -Type String -Force
        Write-RuleResult -RuleId "18.6.14.1" -Title "Ensure 'Hardened UNC Paths' is set to 'Enabled' for NETLOGON and SYSVOL" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.14.1" -Title "Ensure 'Hardened UNC Paths' is set to 'Enabled' for NETLOGON and SYSVOL" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.19.2.1 ──────────────────────────────────────
function Apply-CIS_18_6_19_2_1 {
    <#
    .SYNOPSIS
        Disable IPv6 (Ensure TCPIP6 Parameter 'DisabledComponents' is set to '0xff (255)')
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.19.2.1" -Title "Disable IPv6 (Ensure TCPIP6 Parameter 'DisabledComponents' is set to '0xff (255)')" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Services\TCPIP6\Parameters')) { New-Item -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\TCPIP6\Parameters' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Services\TCPIP6\Parameters' -Name 'DisabledComponents' -Value 255 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.19.2.1" -Title "Disable IPv6 (Ensure TCPIP6 Parameter 'DisabledComponents' is set to '0xff (255)')" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.19.2.1" -Title "Disable IPv6 (Ensure TCPIP6 Parameter 'DisabledComponents' is set to '0xff (255)')" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.20.1 ──────────────────────────────────────
function Apply-CIS_18_6_20_1 {
    <#
    .SYNOPSIS
        Ensure 'Configuration of wireless settings using Windows Connect Now' is set to 'Disabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.20.1" -Title "Ensure 'Configuration of wireless settings using Windows Connect Now' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\WCN\UI')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\WCN\UI' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\WCN\UI' -Name 'DisableWcnUi' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.20.1" -Title "Ensure 'Configuration of wireless settings using Windows Connect Now' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.20.1" -Title "Ensure 'Configuration of wireless settings using Windows Connect Now' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.20.2 ──────────────────────────────────────
function Apply-CIS_18_6_20_2 {
    <#
    .SYNOPSIS
        Ensure 'Prohibit access of the Windows Connect Now wizards' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.20.2" -Title "Ensure 'Prohibit access of the Windows Connect Now wizards' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\WCN\UI')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\WCN\UI' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\WCN\UI' -Name 'DisableWcnUi' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.20.2" -Title "Ensure 'Prohibit access of the Windows Connect Now wizards' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.20.2" -Title "Ensure 'Prohibit access of the Windows Connect Now wizards' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.21.1 ──────────────────────────────────────
function Apply-CIS_18_6_21_1 {
    <#
    .SYNOPSIS
        Ensure 'Minimize the number of simultaneous connections' is set to 3
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.21.1" -Title "Ensure 'Minimize the number of simultaneous connections' is set to 3" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\WCM')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\WCM' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\WCM' -Name 'fMinimizeConnections' -Value 3 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.21.1" -Title "Ensure 'Minimize the number of simultaneous connections' is set to 3" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.21.1" -Title "Ensure 'Minimize the number of simultaneous connections' is set to 3" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.23.2.1 ──────────────────────────────────────
function Apply-CIS_18_6_23_2_1 {
    <#
    .SYNOPSIS
        Ensure 'Allow Windows to automatically connect to suggested open hotspots...' is set to 'Disabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.23.2.1" -Title "Ensure 'Allow Windows to automatically connect to suggested open hotspots...' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Microsoft\WcmSvc\wifinetworkmanager\config')) { New-Item -Path 'HKLM:\SOFTWARE\Microsoft\WcmSvc\wifinetworkmanager\config' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\WcmSvc\wifinetworkmanager\config' -Name 'AutoConnectAllowedOEM' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.23.2.1" -Title "Ensure 'Allow Windows to automatically connect to suggested open hotspots...' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.23.2.1" -Title "Ensure 'Allow Windows to automatically connect to suggested open hotspots...' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.4.1 ──────────────────────────────────────
function Apply-CIS_18_6_4_1 {
    <#
    .SYNOPSIS
        Ensure 'Configure multicast DNS (mDNS) protocol' is set to 'Disabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.4.1" -Title "Ensure 'Configure multicast DNS (mDNS) protocol' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\DNSClient')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\DNSClient' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\DNSClient' -Name 'EnableMulticast' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.4.1" -Title "Ensure 'Configure multicast DNS (mDNS) protocol' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.4.1" -Title "Ensure 'Configure multicast DNS (mDNS) protocol' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.4.2 ──────────────────────────────────────
function Apply-CIS_18_6_4_2 {
    <#
    .SYNOPSIS
        Ensure 'Turn off default IPv6 DNS Servers' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.4.2" -Title "Ensure 'Turn off default IPv6 DNS Servers' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\DNSClient')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\DNSClient' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\DNSClient' -Name 'UsePrimaryDnsSuffixForWpad' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.4.2" -Title "Ensure 'Turn off default IPv6 DNS Servers' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.4.2" -Title "Ensure 'Turn off default IPv6 DNS Servers' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.5.1 ──────────────────────────────────────
function Apply-CIS_18_6_5_1 {
    <#
    .SYNOPSIS
        Ensure 'Enable Font Providers' is set to 'Disabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.5.1" -Title "Ensure 'Enable Font Providers' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\System')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\System' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\System' -Name 'EnableFontProviders' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.5.1" -Title "Ensure 'Enable Font Providers' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.5.1" -Title "Ensure 'Enable Font Providers' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.7.1 ──────────────────────────────────────
function Apply-CIS_18_6_7_1 {
    <#
    .SYNOPSIS
        Ensure 'Audit client does not support encryption' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.7.1" -Title "Ensure 'Audit client does not support encryption' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer' -Name 'AuditClientDoesNotSupportEncryption' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.7.1" -Title "Ensure 'Audit client does not support encryption' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.7.1" -Title "Ensure 'Audit client does not support encryption' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.7.2 ──────────────────────────────────────
function Apply-CIS_18_6_7_2 {
    <#
    .SYNOPSIS
        Ensure 'Audit client does not support signing' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.7.2" -Title "Ensure 'Audit client does not support signing' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer' -Name 'AuditClientDoesNotSupportSigning' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.7.2" -Title "Ensure 'Audit client does not support signing' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.7.2" -Title "Ensure 'Audit client does not support signing' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.7.3 ──────────────────────────────────────
function Apply-CIS_18_6_7_3 {
    <#
    .SYNOPSIS
        Ensure 'Audit insecure guest logon' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.7.3" -Title "Ensure 'Audit insecure guest logon' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer' -Name 'AuditInsecureGuestLogon' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.7.3" -Title "Ensure 'Audit insecure guest logon' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.7.3" -Title "Ensure 'Audit insecure guest logon' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.7.4 ──────────────────────────────────────
function Apply-CIS_18_6_7_4 {
    <#
    .SYNOPSIS
        Ensure 'Enable authentication rate limiter' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.7.4" -Title "Ensure 'Enable authentication rate limiter' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer' -Name 'EnableAuthRateLimiter' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.7.4" -Title "Ensure 'Enable authentication rate limiter' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.7.4" -Title "Ensure 'Enable authentication rate limiter' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.7.5 ──────────────────────────────────────
function Apply-CIS_18_6_7_5 {
    <#
    .SYNOPSIS
        Ensure 'Enable remote mailslots' is set to 'Disabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.7.5" -Title "Ensure 'Enable remote mailslots' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer' -Name 'EnableRemoteMailslots' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.7.5" -Title "Ensure 'Enable remote mailslots' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.7.5" -Title "Ensure 'Enable remote mailslots' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.7.6 ──────────────────────────────────────
function Apply-CIS_18_6_7_6 {
    <#
    .SYNOPSIS
        Ensure 'Mandate the minimum version of SMB' is set to 'Enabled: 3.1.1'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.7.6" -Title "Ensure 'Mandate the minimum version of SMB' is set to 'Enabled: 3.1.1'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer' -Name 'MinSmb2Dialect' -Value 785 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.7.6" -Title "Ensure 'Mandate the minimum version of SMB' is set to 'Enabled: 3.1.1'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.7.6" -Title "Ensure 'Mandate the minimum version of SMB' is set to 'Enabled: 3.1.1'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.7.7 ──────────────────────────────────────
function Apply-CIS_18_6_7_7 {
    <#
    .SYNOPSIS
        Ensure 'Set authentication rate limiter delay' is set to 2000 or more
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.7.7" -Title "Ensure 'Set authentication rate limiter delay' is set to 2000 or more" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanServer' -Name 'AuthRateLimiterDelayInMs' -Value 2000 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.7.7" -Title "Ensure 'Set authentication rate limiter delay' is set to 2000 or more" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.7.7" -Title "Ensure 'Set authentication rate limiter delay' is set to 2000 or more" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.8.1 ──────────────────────────────────────
function Apply-CIS_18_6_8_1 {
    <#
    .SYNOPSIS
        Ensure 'Audit insecure guest logon' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.8.1" -Title "Ensure 'Audit insecure guest logon' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation' -Name 'AuditInsecureGuestLogon' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.8.1" -Title "Ensure 'Audit insecure guest logon' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.8.1" -Title "Ensure 'Audit insecure guest logon' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.8.2 ──────────────────────────────────────
function Apply-CIS_18_6_8_2 {
    <#
    .SYNOPSIS
        Ensure 'Audit server does not support encryption' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.8.2" -Title "Ensure 'Audit server does not support encryption' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation' -Name 'AuditServerDoesNotSupportEncryption' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.8.2" -Title "Ensure 'Audit server does not support encryption' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.8.2" -Title "Ensure 'Audit server does not support encryption' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.8.3 ──────────────────────────────────────
function Apply-CIS_18_6_8_3 {
    <#
    .SYNOPSIS
        Ensure 'Audit server does not support signing' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.8.3" -Title "Ensure 'Audit server does not support signing' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation' -Name 'AuditServerDoesNotSupportSigning' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.8.3" -Title "Ensure 'Audit server does not support signing' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.8.3" -Title "Ensure 'Audit server does not support signing' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.8.4 ──────────────────────────────────────
function Apply-CIS_18_6_8_4 {
    <#
    .SYNOPSIS
        Ensure 'Enable insecure guest logons' is set to 'Disabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.8.4" -Title "Ensure 'Enable insecure guest logons' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation' -Name 'AllowInsecureGuestAuth' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.8.4" -Title "Ensure 'Enable insecure guest logons' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.8.4" -Title "Ensure 'Enable insecure guest logons' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.8.5 ──────────────────────────────────────
function Apply-CIS_18_6_8_5 {
    <#
    .SYNOPSIS
        Ensure 'Enable remote mailslots' is set to 'Disabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.8.5" -Title "Ensure 'Enable remote mailslots' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation' -Name 'EnableRemoteMailslots' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.8.5" -Title "Ensure 'Enable remote mailslots' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.8.5" -Title "Ensure 'Enable remote mailslots' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.8.6 ──────────────────────────────────────
function Apply-CIS_18_6_8_6 {
    <#
    .SYNOPSIS
        Ensure 'Mandate the minimum version of SMB' is set to 'Enabled: 3.1.1'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.8.6" -Title "Ensure 'Mandate the minimum version of SMB' is set to 'Enabled: 3.1.1'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation' -Name 'MinSmb2Dialect' -Value 785 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.8.6" -Title "Ensure 'Mandate the minimum version of SMB' is set to 'Enabled: 3.1.1'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.8.6" -Title "Ensure 'Mandate the minimum version of SMB' is set to 'Enabled: 3.1.1'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.8.7 ──────────────────────────────────────
function Apply-CIS_18_6_8_7 {
    <#
    .SYNOPSIS
        Ensure 'Require Encryption' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.8.7" -Title "Ensure 'Require Encryption' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LanmanWorkstation' -Name 'RequireEncryption' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.8.7" -Title "Ensure 'Require Encryption' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.8.7" -Title "Ensure 'Require Encryption' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.9.1 ──────────────────────────────────────
function Apply-CIS_18_6_9_1 {
    <#
    .SYNOPSIS
        Ensure 'Turn on Mapper I/O (LLTDIO) driver' is set to 'Disabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.9.1" -Title "Ensure 'Turn on Mapper I/O (LLTDIO) driver' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LLTD')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LLTD' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LLTD' -Name 'EnableLLTDIO' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.9.1" -Title "Ensure 'Turn on Mapper I/O (LLTDIO) driver' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.9.1" -Title "Ensure 'Turn on Mapper I/O (LLTDIO) driver' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.6.9.2 ──────────────────────────────────────
function Apply-CIS_18_6_9_2 {
    <#
    .SYNOPSIS
        Ensure 'Turn on Responder (RSPNDR) driver' is set to 'Disabled'
    .NOTES
        Category:       Administrative Templates (Network)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.6.9.2" -Title "Ensure 'Turn on Responder (RSPNDR) driver' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LLTD')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LLTD' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\LLTD' -Name 'EnableRspndr' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.6.9.2" -Title "Ensure 'Turn on Responder (RSPNDR) driver' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.6.9.2" -Title "Ensure 'Turn on Responder (RSPNDR) driver' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.7.1 ──────────────────────────────────────
function Apply-CIS_18_7_1 {
    <#
    .SYNOPSIS
        Ensure 'Allow Print Spooler to accept client connections' is set to 'Disabled'
    .NOTES
        Category:       Administrative Templates (Printers, Start Menu)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.7.1" -Title "Ensure 'Allow Print Spooler to accept client connections' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers' -Name 'RegisterSpoolerRemoteRpcEndPoint' -Value 2 -Type DWord -Force
        Write-RuleResult -RuleId "18.7.1" -Title "Ensure 'Allow Print Spooler to accept client connections' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.7.1" -Title "Ensure 'Allow Print Spooler to accept client connections' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.7.10 ──────────────────────────────────────
function Apply-CIS_18_7_10 {
    <#
    .SYNOPSIS
        Ensure 'Limits print driver installation to Administrators' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Printers, Start Menu)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.7.10" -Title "Ensure 'Limits print driver installation to Administrators' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\PointAndPrint')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\PointAndPrint' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\PointAndPrint' -Name 'RestrictDriverInstallationToAdministrators' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.7.10" -Title "Ensure 'Limits print driver installation to Administrators' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.7.10" -Title "Ensure 'Limits print driver installation to Administrators' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.7.11 ──────────────────────────────────────
function Apply-CIS_18_7_11 {
    <#
    .SYNOPSIS
        Ensure 'Manage processing of Queue-specific files' is set to 'Enabled: Limit Queue-specific files to Color profiles'
    .NOTES
        Category:       Administrative Templates (Printers, Start Menu)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.7.11" -Title "Ensure 'Manage processing of Queue-specific files' is set to 'Enabled: Limit Queue-specific files to Color profiles'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers' -Name 'QueueSpecificFiles' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.7.11" -Title "Ensure 'Manage processing of Queue-specific files' is set to 'Enabled: Limit Queue-specific files to Color profiles'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.7.11" -Title "Ensure 'Manage processing of Queue-specific files' is set to 'Enabled: Limit Queue-specific files to Color profiles'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.7.12 ──────────────────────────────────────
function Apply-CIS_18_7_12 {
    <#
    .SYNOPSIS
        Ensure 'Point and Print Restrictions: When installing drivers for a new connection' is set to 'Enabled: Show warning and elevation prompt'
    .NOTES
        Category:       Administrative Templates (Printers, Start Menu)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.7.12" -Title "Ensure 'Point and Print Restrictions: When installing drivers for a new connection' is set to 'Enabled: Show warning and elevation prompt'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\PointAndPrint')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\PointAndPrint' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\PointAndPrint' -Name 'InForestPrintSecurity' -Value 2 -Type DWord -Force
        Write-RuleResult -RuleId "18.7.12" -Title "Ensure 'Point and Print Restrictions: When installing drivers for a new connection' is set to 'Enabled: Show warning and elevation prompt'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.7.12" -Title "Ensure 'Point and Print Restrictions: When installing drivers for a new connection' is set to 'Enabled: Show warning and elevation prompt'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.7.13 ──────────────────────────────────────
function Apply-CIS_18_7_13 {
    <#
    .SYNOPSIS
        Ensure 'Point and Print Restrictions: When updating drivers for an existing connection' is set to 'Enabled: Show warning and elevation prompt'
    .NOTES
        Category:       Administrative Templates (Printers, Start Menu)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.7.13" -Title "Ensure 'Point and Print Restrictions: When updating drivers for an existing connection' is set to 'Enabled: Show warning and elevation prompt'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\PointAndPrint')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\PointAndPrint' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\PointAndPrint' -Name 'UpdatePromptSettings' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.7.13" -Title "Ensure 'Point and Print Restrictions: When updating drivers for an existing connection' is set to 'Enabled: Show warning and elevation prompt'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.7.13" -Title "Ensure 'Point and Print Restrictions: When updating drivers for an existing connection' is set to 'Enabled: Show warning and elevation prompt'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.7.2 ──────────────────────────────────────
function Apply-CIS_18_7_2 {
    <#
    .SYNOPSIS
        Ensure 'Configure Redirection Guard' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Printers, Start Menu)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.7.2" -Title "Ensure 'Configure Redirection Guard' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers' -Name 'ConfigureRedirectionGuard' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.7.2" -Title "Ensure 'Configure Redirection Guard' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.7.2" -Title "Ensure 'Configure Redirection Guard' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.7.3 ──────────────────────────────────────
function Apply-CIS_18_7_3 {
    <#
    .SYNOPSIS
        Ensure 'Configure RPC connection settings: Protocol to use for outgoing RPC connections' is set to 'Enabled: RPC over TCP'
    .NOTES
        Category:       Administrative Templates (Printers, Start Menu)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.7.3" -Title "Ensure 'Configure RPC connection settings: Protocol to use for outgoing RPC connections' is set to 'Enabled: RPC over TCP'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\RPC')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\RPC' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\RPC' -Name 'RpcUseNamedPipeProtocol' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.7.3" -Title "Ensure 'Configure RPC connection settings: Protocol to use for outgoing RPC connections' is set to 'Enabled: RPC over TCP'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.7.3" -Title "Ensure 'Configure RPC connection settings: Protocol to use for outgoing RPC connections' is set to 'Enabled: RPC over TCP'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.7.4 ──────────────────────────────────────
function Apply-CIS_18_7_4 {
    <#
    .SYNOPSIS
        Ensure 'Configure RPC connection settings: Use authentication for outgoing RPC connections' is set to 'Enabled: Default'
    .NOTES
        Category:       Administrative Templates (Printers, Start Menu)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.7.4" -Title "Ensure 'Configure RPC connection settings: Use authentication for outgoing RPC connections' is set to 'Enabled: Default'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\RPC')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\RPC' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\RPC' -Name 'RpcProtocols' -Value 7 -Type DWord -Force
        Write-RuleResult -RuleId "18.7.4" -Title "Ensure 'Configure RPC connection settings: Use authentication for outgoing RPC connections' is set to 'Enabled: Default'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.7.4" -Title "Ensure 'Configure RPC connection settings: Use authentication for outgoing RPC connections' is set to 'Enabled: Default'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.7.5 ──────────────────────────────────────
function Apply-CIS_18_7_5 {
    <#
    .SYNOPSIS
        Ensure 'Configure RPC listener settings: Protocols to allow for incoming RPC connections' is set to 'Enabled: RPC over TCP'
    .NOTES
        Category:       Administrative Templates (Printers, Start Menu)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.7.5" -Title "Ensure 'Configure RPC listener settings: Protocols to allow for incoming RPC connections' is set to 'Enabled: RPC over TCP'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\RPC')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\RPC' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\RPC' -Name 'ForceKerberosForRpcConnections' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.7.5" -Title "Ensure 'Configure RPC listener settings: Protocols to allow for incoming RPC connections' is set to 'Enabled: RPC over TCP'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.7.5" -Title "Ensure 'Configure RPC listener settings: Protocols to allow for incoming RPC connections' is set to 'Enabled: RPC over TCP'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.7.6 ──────────────────────────────────────
function Apply-CIS_18_7_6 {
    <#
    .SYNOPSIS
        Ensure 'Configure RPC listener settings: Authentication protocol to use for incoming RPC connections:' is set to 'Enabled: Negotiate' or higher
    .NOTES
        Category:       Administrative Templates (Printers, Start Menu)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.7.6" -Title "Ensure 'Configure RPC listener settings: Authentication protocol to use for incoming RPC connections:' is set to 'Enabled: Negotiate' or higher" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\RPC')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\RPC' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\RPC' -Name 'ForceAuthenticationForRpcConnections' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.7.6" -Title "Ensure 'Configure RPC listener settings: Authentication protocol to use for incoming RPC connections:' is set to 'Enabled: Negotiate' or higher" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.7.6" -Title "Ensure 'Configure RPC listener settings: Authentication protocol to use for incoming RPC connections:' is set to 'Enabled: Negotiate' or higher" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.7.7 ──────────────────────────────────────
function Apply-CIS_18_7_7 {
    <#
    .SYNOPSIS
        Ensure 'Configure RPC over TCP port' is set to 'Enabled: 0'
    .NOTES
        Category:       Administrative Templates (Printers, Start Menu)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.7.7" -Title "Ensure 'Configure RPC over TCP port' is set to 'Enabled: 0'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\RPC')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\RPC' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\RPC' -Name 'RpcTcpPort' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.7.7" -Title "Ensure 'Configure RPC over TCP port' is set to 'Enabled: 0'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.7.7" -Title "Ensure 'Configure RPC over TCP port' is set to 'Enabled: 0'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.7.8 ──────────────────────────────────────
function Apply-CIS_18_7_8 {
    <#
    .SYNOPSIS
        Ensure 'Configure RPC packet level privacy setting for incoming connections' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Printers, Start Menu)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.7.8" -Title "Ensure 'Configure RPC packet level privacy setting for incoming connections' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\RPC')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\RPC' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\RPC' -Name 'RpcAuthenticationLevel' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.7.8" -Title "Ensure 'Configure RPC packet level privacy setting for incoming connections' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.7.8" -Title "Ensure 'Configure RPC packet level privacy setting for incoming connections' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.7.9 ──────────────────────────────────────
function Apply-CIS_18_7_9 {
    <#
    .SYNOPSIS
        Ensure 'Configure Windows protected print' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Printers, Start Menu)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.7.9" -Title "Ensure 'Configure Windows protected print' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\Wpp')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\Wpp' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers\Wpp' -Name 'WindowsProtectedPrint' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.7.9" -Title "Ensure 'Configure Windows protected print' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.7.9" -Title "Ensure 'Configure Windows protected print' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.8.1.1 ──────────────────────────────────────
function Apply-CIS_18_8_1_1 {
    <#
    .SYNOPSIS
        Ensure 'Turn off notifications network usage' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Printers, Start Menu)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.8.1.1" -Title "Ensure 'Turn off notifications network usage' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\CurrentVersion\PushNotifications')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\CurrentVersion\PushNotifications' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\CurrentVersion\PushNotifications' -Name 'NoCloudApplicationNotification' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.8.1.1" -Title "Ensure 'Turn off notifications network usage' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.8.1.1" -Title "Ensure 'Turn off notifications network usage' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.8.2 ──────────────────────────────────────
function Apply-CIS_18_8_2 {
    <#
    .SYNOPSIS
        Ensure 'Remove Personalized Website Recommendations from the Recommended section in the Start Menu' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (Printers, Start Menu)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.8.2" -Title "Ensure 'Remove Personalized Website Recommendations from the Recommended section in the Start Menu' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Explorer')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Explorer' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Explorer' -Name 'HideRecommendedWebsitesInStart' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.8.2" -Title "Ensure 'Remove Personalized Website Recommendations from the Recommended section in the Start Menu' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.8.2" -Title "Ensure 'Remove Personalized Website Recommendations from the Recommended section in the Start Menu' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.13.1 ──────────────────────────────────────
function Apply-CIS_18_9_13_1 {
    <#
    .SYNOPSIS
        Ensure 'Boot-Start Driver Initialization Policy' is set to 'Enabled: Good, unknown and bad but critical'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.13.1" -Title "Ensure 'Boot-Start Driver Initialization Policy' is set to 'Enabled: Good, unknown and bad but critical'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SYSTEM\CurrentControlSet\Policies\EarlyLaunch')) { New-Item -Path 'HKLM:\SYSTEM\CurrentControlSet\Policies\EarlyLaunch' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Policies\EarlyLaunch' -Name 'DriverLoadPolicy' -Value 3 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.13.1" -Title "Ensure 'Boot-Start Driver Initialization Policy' is set to 'Enabled: Good, unknown and bad but critical'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.13.1" -Title "Ensure 'Boot-Start Driver Initialization Policy' is set to 'Enabled: Good, unknown and bad but critical'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.19.2 ──────────────────────────────────────
function Apply-CIS_18_9_19_2 {
    <#
    .SYNOPSIS
        Ensure 'Continue experiences on this device' is set to 'Disabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.19.2" -Title "Ensure 'Continue experiences on this device' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\System')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\System' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\System' -Name 'EnableCdp' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.19.2" -Title "Ensure 'Continue experiences on this device' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.19.2" -Title "Ensure 'Continue experiences on this device' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.20.1.1 ──────────────────────────────────────
function Apply-CIS_18_9_20_1_1 {
    <#
    .SYNOPSIS
        Ensure 'Turn off access to the Store' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.20.1.1" -Title "Ensure 'Turn off access to the Store' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsStore')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsStore' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsStore' -Name 'RemoveWindowsStore' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.20.1.1" -Title "Ensure 'Turn off access to the Store' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.20.1.1" -Title "Ensure 'Turn off access to the Store' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.20.1.10 ──────────────────────────────────────
function Apply-CIS_18_9_20_1_10 {
    <#
    .SYNOPSIS
        Ensure 'Turn off the "Order Prints" picture task' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.20.1.10" -Title "Ensure 'Turn off the `"Order Prints`" picture task' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Explorer')) { New-Item -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Explorer' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Explorer' -Name 'NoOnlinePrintsWizard' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.20.1.10" -Title "Ensure 'Turn off the `"Order Prints`" picture task' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.20.1.10" -Title "Ensure 'Turn off the `"Order Prints`" picture task' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.20.1.11 ──────────────────────────────────────
function Apply-CIS_18_9_20_1_11 {
    <#
    .SYNOPSIS
        Ensure 'Turn off the "Publish to Web" task for files and folders' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.20.1.11" -Title "Ensure 'Turn off the `"Publish to Web`" task for files and folders' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Explorer')) { New-Item -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Explorer' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Explorer' -Name 'NoPublishingWizard' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.20.1.11" -Title "Ensure 'Turn off the `"Publish to Web`" task for files and folders' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.20.1.11" -Title "Ensure 'Turn off the `"Publish to Web`" task for files and folders' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.20.1.12 ──────────────────────────────────────
function Apply-CIS_18_9_20_1_12 {
    <#
    .SYNOPSIS
        Ensure 'Turn off the Windows Messenger Customer Experience Improvement Program' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.20.1.12" -Title "Ensure 'Turn off the Windows Messenger Customer Experience Improvement Program' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Messenger\Client')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Messenger\Client' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Messenger\Client' -Name 'CEIP' -Value 2 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.20.1.12" -Title "Ensure 'Turn off the Windows Messenger Customer Experience Improvement Program' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.20.1.12" -Title "Ensure 'Turn off the Windows Messenger Customer Experience Improvement Program' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.20.1.13 ──────────────────────────────────────
function Apply-CIS_18_9_20_1_13 {
    <#
    .SYNOPSIS
        Ensure 'Turn off Windows Customer Experience Improvement Program' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.20.1.13" -Title "Ensure 'Turn off Windows Customer Experience Improvement Program' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\SQMClient\Windows')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\SQMClient\Windows' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\SQMClient\Windows' -Name 'CEIPEnable' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.20.1.13" -Title "Ensure 'Turn off Windows Customer Experience Improvement Program' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.20.1.13" -Title "Ensure 'Turn off Windows Customer Experience Improvement Program' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.20.1.14 ──────────────────────────────────────
function Apply-CIS_18_9_20_1_14 {
    <#
    .SYNOPSIS
        Ensure 'Turn off Windows Error Reporting' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.20.1.14" -Title "Ensure 'Turn off Windows Error Reporting' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Windows Error Reporting')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Windows Error Reporting' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Windows Error Reporting' -Name 'Disabled' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.20.1.14" -Title "Ensure 'Turn off Windows Error Reporting' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.20.1.14" -Title "Ensure 'Turn off Windows Error Reporting' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.20.1.2 ──────────────────────────────────────
function Apply-CIS_18_9_20_1_2 {
    <#
    .SYNOPSIS
        Ensure 'Turn off downloading of print drivers over HTTP' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.20.1.2" -Title "Ensure 'Turn off downloading of print drivers over HTTP' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers' -Name 'DisableWebPnPDownload' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.20.1.2" -Title "Ensure 'Turn off downloading of print drivers over HTTP' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.20.1.2" -Title "Ensure 'Turn off downloading of print drivers over HTTP' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.20.1.3 ──────────────────────────────────────
function Apply-CIS_18_9_20_1_3 {
    <#
    .SYNOPSIS
        Ensure 'Turn off handwriting personalization data sharing' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.20.1.3" -Title "Ensure 'Turn off handwriting personalization data sharing' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\TabletPC')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\TabletPC' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\TabletPC' -Name 'PreventHandwritingDataSharing' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.20.1.3" -Title "Ensure 'Turn off handwriting personalization data sharing' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.20.1.3" -Title "Ensure 'Turn off handwriting personalization data sharing' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.20.1.4 ──────────────────────────────────────
function Apply-CIS_18_9_20_1_4 {
    <#
    .SYNOPSIS
        Ensure 'Turn off handwriting recognition error reporting' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.20.1.4" -Title "Ensure 'Turn off handwriting recognition error reporting' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\HandwritingErrorReports')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\HandwritingErrorReports' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\HandwritingErrorReports' -Name 'PreventHandwritingErrorReports' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.20.1.4" -Title "Ensure 'Turn off handwriting recognition error reporting' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.20.1.4" -Title "Ensure 'Turn off handwriting recognition error reporting' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.20.1.5 ──────────────────────────────────────
function Apply-CIS_18_9_20_1_5 {
    <#
    .SYNOPSIS
        Ensure 'Turn off Internet Connection Wizard if URL connection is referring to Microsoft.com' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.20.1.5" -Title "Ensure 'Turn off Internet Connection Wizard if URL connection is referring to Microsoft.com' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Internet Explorer\Main')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Internet Explorer\Main' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Internet Explorer\Main' -Name 'AlwaysUseMyDestinations' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.20.1.5" -Title "Ensure 'Turn off Internet Connection Wizard if URL connection is referring to Microsoft.com' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.20.1.5" -Title "Ensure 'Turn off Internet Connection Wizard if URL connection is referring to Microsoft.com' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.20.1.6 ──────────────────────────────────────
function Apply-CIS_18_9_20_1_6 {
    <#
    .SYNOPSIS
        Ensure 'Turn off Internet download for Web publishing and online ordering wizards' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.20.1.6" -Title "Ensure 'Turn off Internet download for Web publishing and online ordering wizards' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Explorer')) { New-Item -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Explorer' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\Explorer' -Name 'NoWebServices' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.20.1.6" -Title "Ensure 'Turn off Internet download for Web publishing and online ordering wizards' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.20.1.6" -Title "Ensure 'Turn off Internet download for Web publishing and online ordering wizards' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.20.1.7 ──────────────────────────────────────
function Apply-CIS_18_9_20_1_7 {
    <#
    .SYNOPSIS
        Ensure 'Turn off printing over HTTP' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.20.1.7" -Title "Ensure 'Turn off printing over HTTP' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Printers' -Name 'DisableHTTPPrinting' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.20.1.7" -Title "Ensure 'Turn off printing over HTTP' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.20.1.7" -Title "Ensure 'Turn off printing over HTTP' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.20.1.8 ──────────────────────────────────────
function Apply-CIS_18_9_20_1_8 {
    <#
    .SYNOPSIS
        Ensure 'Turn off Registration if URL connection is referring to Microsoft.com' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.20.1.8" -Title "Ensure 'Turn off Registration if URL connection is referring to Microsoft.com' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Registration Wizard Control')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Registration Wizard Control' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Registration Wizard Control' -Name 'NoRegistration' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.20.1.8" -Title "Ensure 'Turn off Registration if URL connection is referring to Microsoft.com' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.20.1.8" -Title "Ensure 'Turn off Registration if URL connection is referring to Microsoft.com' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.20.1.9 ──────────────────────────────────────
function Apply-CIS_18_9_20_1_9 {
    <#
    .SYNOPSIS
        Ensure 'Turn off Search Companion content file updates' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.20.1.9" -Title "Ensure 'Turn off Search Companion content file updates' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\SearchCompanion')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\SearchCompanion' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\SearchCompanion' -Name 'DisableContentFileUpdates' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.20.1.9" -Title "Ensure 'Turn off Search Companion content file updates' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.20.1.9" -Title "Ensure 'Turn off Search Companion content file updates' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.3.1 ──────────────────────────────────────
function Apply-CIS_18_9_3_1 {
    <#
    .SYNOPSIS
        Ensure 'Include command line in process creation events' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.3.1" -Title "Ensure 'Include command line in process creation events' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System\Audit')) { New-Item -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System\Audit' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System\Audit' -Name 'ProcessCreationIncludeCmdLine_Enabled' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.3.1" -Title "Ensure 'Include command line in process creation events' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.3.1" -Title "Ensure 'Include command line in process creation events' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.4.1 ──────────────────────────────────────
function Apply-CIS_18_9_4_1 {
    <#
    .SYNOPSIS
        Ensure 'Encryption Oracle Remediation' is set to 'Enabled: Force Updated Clients'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.4.1" -Title "Ensure 'Encryption Oracle Remediation' is set to 'Enabled: Force Updated Clients'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System\CredSSP\Parameters')) { New-Item -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System\CredSSP\Parameters' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Microsoft\Windows\CurrentVersion\Policies\System\CredSSP\Parameters' -Name 'AllowEncryptionOracle' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.4.1" -Title "Ensure 'Encryption Oracle Remediation' is set to 'Enabled: Force Updated Clients'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.4.1" -Title "Ensure 'Encryption Oracle Remediation' is set to 'Enabled: Force Updated Clients'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.4.2 ──────────────────────────────────────
function Apply-CIS_18_9_4_2 {
    <#
    .SYNOPSIS
        Ensure 'Remote host allows delegation of non-exportable credentials' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.4.2" -Title "Ensure 'Remote host allows delegation of non-exportable credentials' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\CredentialsDelegation')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\CredentialsDelegation' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\CredentialsDelegation' -Name 'AllowProtectedCreds' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.4.2" -Title "Ensure 'Remote host allows delegation of non-exportable credentials' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.4.2" -Title "Ensure 'Remote host allows delegation of non-exportable credentials' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.5.1 ──────────────────────────────────────
function Apply-CIS_18_9_5_1 {
    <#
    .SYNOPSIS
        Ensure 'Turn On Virtualization Based Security' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.5.1" -Title "Ensure 'Turn On Virtualization Based Security' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard' -Name 'EnableVirtualizationBasedSecurity' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.5.1" -Title "Ensure 'Turn On Virtualization Based Security' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.5.1" -Title "Ensure 'Turn On Virtualization Based Security' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.5.2 ──────────────────────────────────────
function Apply-CIS_18_9_5_2 {
    <#
    .SYNOPSIS
        Ensure 'Turn On Virtualization Based Security: Select Platform Security Level' is set to 'Secure Boot' or higher
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.5.2" -Title "Ensure 'Turn On Virtualization Based Security: Select Platform Security Level' is set to 'Secure Boot' or higher" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard' -Name 'RequirePlatformSecurityFeatures' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.5.2" -Title "Ensure 'Turn On Virtualization Based Security: Select Platform Security Level' is set to 'Secure Boot' or higher" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.5.2" -Title "Ensure 'Turn On Virtualization Based Security: Select Platform Security Level' is set to 'Secure Boot' or higher" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.5.3 ──────────────────────────────────────
function Apply-CIS_18_9_5_3 {
    <#
    .SYNOPSIS
        Ensure 'Turn On Virtualization Based Security: Virtualization Based Protection of Code Integrity' is set to 'Enabled with UEFI lock'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.5.3" -Title "Ensure 'Turn On Virtualization Based Security: Virtualization Based Protection of Code Integrity' is set to 'Enabled with UEFI lock'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard' -Name 'HypervisorEnforcedCodeIntegrity' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.5.3" -Title "Ensure 'Turn On Virtualization Based Security: Virtualization Based Protection of Code Integrity' is set to 'Enabled with UEFI lock'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.5.3" -Title "Ensure 'Turn On Virtualization Based Security: Virtualization Based Protection of Code Integrity' is set to 'Enabled with UEFI lock'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.5.4 ──────────────────────────────────────
function Apply-CIS_18_9_5_4 {
    <#
    .SYNOPSIS
        Ensure 'Turn On Virtualization Based Security: Require UEFI Memory Attributes Table' is set to 'True (checked)'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.5.4" -Title "Ensure 'Turn On Virtualization Based Security: Require UEFI Memory Attributes Table' is set to 'True (checked)'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard' -Name 'HviRequireUefiMemoryAttributesTable' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.5.4" -Title "Ensure 'Turn On Virtualization Based Security: Require UEFI Memory Attributes Table' is set to 'True (checked)'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.5.4" -Title "Ensure 'Turn On Virtualization Based Security: Require UEFI Memory Attributes Table' is set to 'True (checked)'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.5.5 ──────────────────────────────────────
function Apply-CIS_18_9_5_5 {
    <#
    .SYNOPSIS
        Ensure 'Turn On Virtualization Based Security: Credential Guard Configuration' is set to 'Enabled with UEFI lock'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.5.5" -Title "Ensure 'Turn On Virtualization Based Security: Credential Guard Configuration' is set to 'Enabled with UEFI lock'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard' -Name 'LsaCfgFlags' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.5.5" -Title "Ensure 'Turn On Virtualization Based Security: Credential Guard Configuration' is set to 'Enabled with UEFI lock'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.5.5" -Title "Ensure 'Turn On Virtualization Based Security: Credential Guard Configuration' is set to 'Enabled with UEFI lock'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.5.6 ──────────────────────────────────────
function Apply-CIS_18_9_5_6 {
    <#
    .SYNOPSIS
        Ensure 'Turn On Virtualization Based Security: Secure Launch Configuration' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.5.6" -Title "Ensure 'Turn On Virtualization Based Security: Secure Launch Configuration' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard' -Name 'ConfigureSystemGuardLaunch' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.5.6" -Title "Ensure 'Turn On Virtualization Based Security: Secure Launch Configuration' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.5.6" -Title "Ensure 'Turn On Virtualization Based Security: Secure Launch Configuration' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.5.7 ──────────────────────────────────────
function Apply-CIS_18_9_5_7 {
    <#
    .SYNOPSIS
        Ensure 'Turn On Virtualization Based Security: Kernel-mode Hardware-enforced Stack Protection' is set to 'Enabled: Enabled in enforcement mode'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.5.7" -Title "Ensure 'Turn On Virtualization Based Security: Kernel-mode Hardware-enforced Stack Protection' is set to 'Enabled: Enabled in enforcement mode'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceGuard' -Name 'HwEnforcedStackProtection' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.5.7" -Title "Ensure 'Turn On Virtualization Based Security: Kernel-mode Hardware-enforced Stack Protection' is set to 'Enabled: Enabled in enforcement mode'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.5.7" -Title "Ensure 'Turn On Virtualization Based Security: Kernel-mode Hardware-enforced Stack Protection' is set to 'Enabled: Enabled in enforcement mode'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.7.1.1 ──────────────────────────────────────
function Apply-CIS_18_9_7_1_1 {
    <#
    .SYNOPSIS
        Ensure 'Prevent installation of devices using drivers that match these device setup classes' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.7.1.1" -Title "Ensure 'Prevent installation of devices using drivers that match these device setup classes' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceInstall\Restrictions')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceInstall\Restrictions' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceInstall\Restrictions' -Name 'DenyDeviceClasses' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.7.1.1" -Title "Ensure 'Prevent installation of devices using drivers that match these device setup classes' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.7.1.1" -Title "Ensure 'Prevent installation of devices using drivers that match these device setup classes' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.7.1.2 ──────────────────────────────────────
function Apply-CIS_18_9_7_1_2 {
    <#
    .SYNOPSIS
        Ensure 'Prevent installation of devices using drivers for these device setup' is set to 'IEEE 1394 device setup classes'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.7.1.2" -Title "Ensure 'Prevent installation of devices using drivers for these device setup' is set to 'IEEE 1394 device setup classes'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceInstall\Restrictions\DenyDeviceClasses')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceInstall\Restrictions\DenyDeviceClasses' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceInstall\Restrictions\DenyDeviceClasses' -Name '1' -Value '{6bdd1fc1-810f-11d0-bec7-08002be2092f}' -Type String -Force
        Write-RuleResult -RuleId "18.9.7.1.2" -Title "Ensure 'Prevent installation of devices using drivers for these device setup' is set to 'IEEE 1394 device setup classes'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.7.1.2" -Title "Ensure 'Prevent installation of devices using drivers for these device setup' is set to 'IEEE 1394 device setup classes'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.7.1.3 ──────────────────────────────────────
function Apply-CIS_18_9_7_1_3 {
    <#
    .SYNOPSIS
        Ensure 'Also apply to matching devices that are already installed.' is set to 'True'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.7.1.3" -Title "Ensure 'Also apply to matching devices that are already installed.' is set to 'True'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceInstall\Restrictions')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceInstall\Restrictions' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\DeviceInstall\Restrictions' -Name 'DenyDeviceClassesRetroactive' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.7.1.3" -Title "Ensure 'Also apply to matching devices that are already installed.' is set to 'True'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.7.1.3" -Title "Ensure 'Also apply to matching devices that are already installed.' is set to 'True'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 18.9.7.2 ──────────────────────────────────────
function Apply-CIS_18_9_7_2 {
    <#
    .SYNOPSIS
        Ensure 'Prevent device metadata retrieval from the Internet' is set to 'Enabled'
    .NOTES
        Category:       Administrative Templates (System)
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "18.9.7.2" -Title "Ensure 'Prevent device metadata retrieval from the Internet' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Device Metadata')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Device Metadata' -Force -ErrorAction SilentlyContinue }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\Windows\Device Metadata' -Name 'PreventDeviceMetadataFromNetwork' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "18.9.7.2" -Title "Ensure 'Prevent device metadata retrieval from the Internet' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "18.9.7.2" -Title "Ensure 'Prevent device metadata retrieval from the Internet' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 1.1.1 ──────────────────────────────────────
function Apply-CIS_1_1_1 {
    <#
    .SYNOPSIS
        Ensure 'Enforce password history' is set to '24 or more password(s)'
    .NOTES
        Category:       Account Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "1.1.1" -Title "Ensure 'Enforce password history' is set to '24 or more password(s)'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        $seceditExport = "$env:TEMP\secpol_export.cfg"
        $seceditImport = "$env:TEMP\secpol_import.cfg"
        
        # Export current security policy
        secedit /export /cfg $seceditExport /quiet
        
        # Read and modify
        $content = Get-Content $seceditExport
        $content = $content -replace 'PasswordHistorySize\s*=\s*\d+', 'PasswordHistorySize = 24'
        
        # If setting doesn't exist, add it
        if ($content -notmatch 'PasswordHistorySize') {
            $content = $content -replace '(\[System Access\])', "`$1`nPasswordHistorySize = 24"
        }
        
        $content | Set-Content $seceditImport -Force
        
        # Import modified policy
        secedit /configure /db secedit.sdb /cfg $seceditImport /quiet
        
        # Cleanup
        Remove-Item $seceditExport, $seceditImport -Force -ErrorAction SilentlyContinue
        Write-RuleResult -RuleId "1.1.1" -Title "Ensure 'Enforce password history' is set to '24 or more password(s)'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "1.1.1" -Title "Ensure 'Enforce password history' is set to '24 or more password(s)'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 1.1.2 ──────────────────────────────────────
function Apply-CIS_1_1_2 {
    <#
    .SYNOPSIS
        Ensure 'Maximum password age' is set to '365 or fewer days, but not 0'
    .NOTES
        Category:       Account Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "1.1.2" -Title "Ensure 'Maximum password age' is set to '365 or fewer days, but not 0'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        $seceditExport = "$env:TEMP\secpol_export.cfg"
        $seceditImport = "$env:TEMP\secpol_import.cfg"
        secedit /export /cfg $seceditExport /quiet
        $content = Get-Content $seceditExport
        $content = $content -replace 'MaximumPasswordAge\s*=\s*\d+', 'MaximumPasswordAge = 365'
        if ($content -notmatch 'MaximumPasswordAge') {
            $content = $content -replace '(\[System Access\])', "`$1`nMaximumPasswordAge = 365"
        }
        $content | Set-Content $seceditImport -Force
        secedit /configure /db secedit.sdb /cfg $seceditImport /quiet
        Remove-Item $seceditExport, $seceditImport -Force -ErrorAction SilentlyContinue
        Write-RuleResult -RuleId "1.1.2" -Title "Ensure 'Maximum password age' is set to '365 or fewer days, but not 0'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "1.1.2" -Title "Ensure 'Maximum password age' is set to '365 or fewer days, but not 0'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 1.1.3 ──────────────────────────────────────
function Apply-CIS_1_1_3 {
    <#
    .SYNOPSIS
        Ensure 'Minimum password age' is set to '1 or more day(s)'
    .NOTES
        Category:       Account Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "1.1.3" -Title "Ensure 'Minimum password age' is set to '1 or more day(s)'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        $seceditExport = "$env:TEMP\secpol_export.cfg"
        $seceditImport = "$env:TEMP\secpol_import.cfg"
        secedit /export /cfg $seceditExport /quiet
        $content = Get-Content $seceditExport
        $content = $content -replace 'MinimumPasswordAge\s*=\s*\d+', 'MinimumPasswordAge = 1'
        if ($content -notmatch 'MinimumPasswordAge') {
            $content = $content -replace '(\[System Access\])', "`$1`nMinimumPasswordAge = 1"
        }
        $content | Set-Content $seceditImport -Force
        secedit /configure /db secedit.sdb /cfg $seceditImport /quiet
        Remove-Item $seceditExport, $seceditImport -Force -ErrorAction SilentlyContinue
        Write-RuleResult -RuleId "1.1.3" -Title "Ensure 'Minimum password age' is set to '1 or more day(s)'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "1.1.3" -Title "Ensure 'Minimum password age' is set to '1 or more day(s)'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 1.1.4 ──────────────────────────────────────
function Apply-CIS_1_1_4 {
    <#
    .SYNOPSIS
        Ensure 'Minimum password length' is set to '14 or more character(s)'
    .NOTES
        Category:       Account Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "1.1.4" -Title "Ensure 'Minimum password length' is set to '14 or more character(s)'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        $seceditExport = "$env:TEMP\secpol_export.cfg"
        $seceditImport = "$env:TEMP\secpol_import.cfg"
        secedit /export /cfg $seceditExport /quiet
        $content = Get-Content $seceditExport
        $content = $content -replace 'MinimumPasswordLength\s*=\s*\d+', 'MinimumPasswordLength = 14'
        if ($content -notmatch 'MinimumPasswordLength') {
            $content = $content -replace '(\[System Access\])', "`$1`nMinimumPasswordLength = 14"
        }
        $content | Set-Content $seceditImport -Force
        secedit /configure /db secedit.sdb /cfg $seceditImport /quiet
        Remove-Item $seceditExport, $seceditImport -Force -ErrorAction SilentlyContinue
        Write-RuleResult -RuleId "1.1.4" -Title "Ensure 'Minimum password length' is set to '14 or more character(s)'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "1.1.4" -Title "Ensure 'Minimum password length' is set to '14 or more character(s)'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 1.1.5 ──────────────────────────────────────
function Apply-CIS_1_1_5 {
    <#
    .SYNOPSIS
        Ensure 'Password must meet complexity requirements' is set to 'Enabled'
    .NOTES
        Category:       Account Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "1.1.5" -Title "Ensure 'Password must meet complexity requirements' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        $seceditExport = "$env:TEMP\secpol_export.cfg"
        $seceditImport = "$env:TEMP\secpol_import.cfg"
        secedit /export /cfg $seceditExport /quiet
        $content = Get-Content $seceditExport
        $content = $content -replace 'PasswordComplexity\s*=\s*\d+', 'PasswordComplexity = 1'
        if ($content -notmatch 'PasswordComplexity') {
            $content = $content -replace '(\[System Access\])', "`$1`nPasswordComplexity = 1"
        }
        $content | Set-Content $seceditImport -Force
        secedit /configure /db secedit.sdb /cfg $seceditImport /quiet
        Remove-Item $seceditExport, $seceditImport -Force -ErrorAction SilentlyContinue
        Write-RuleResult -RuleId "1.1.5" -Title "Ensure 'Password must meet complexity requirements' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "1.1.5" -Title "Ensure 'Password must meet complexity requirements' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 1.1.6 ──────────────────────────────────────
function Apply-CIS_1_1_6 {
    <#
    .SYNOPSIS
        Ensure 'Relax minimum password length limits' is set to 'Enabled'
    .NOTES
        Category:       Account Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "1.1.6" -Title "Ensure 'Relax minimum password length limits' is set to 'Enabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\System\CurrentControlSet\Control\SAM')) {
            New-Item -Path 'HKLM:\System\CurrentControlSet\Control\SAM' -Force | Out-Null
        }
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\SAM' -Name 'RelaxMinimumPasswordLengthLimits' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "1.1.6" -Title "Ensure 'Relax minimum password length limits' is set to 'Enabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "1.1.6" -Title "Ensure 'Relax minimum password length limits' is set to 'Enabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 1.1.7 ──────────────────────────────────────
function Apply-CIS_1_1_7 {
    <#
    .SYNOPSIS
        Ensure 'Store passwords using reversible encryption' is set to 'Disabled'
    .NOTES
        Category:       Account Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "1.1.7" -Title "Ensure 'Store passwords using reversible encryption' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        $seceditExport = "$env:TEMP\secpol_export.cfg"
        $seceditImport = "$env:TEMP\secpol_import.cfg"
        secedit /export /cfg $seceditExport /quiet
        $content = Get-Content $seceditExport
        $content = $content -replace 'ClearTextPassword\s*=\s*\d+', 'ClearTextPassword = 0'
        if ($content -notmatch 'ClearTextPassword') {
            $content = $content -replace '(\[System Access\])', "`$1`nClearTextPassword = 0"
        }
        $content | Set-Content $seceditImport -Force
        secedit /configure /db secedit.sdb /cfg $seceditImport /quiet
        Remove-Item $seceditExport, $seceditImport -Force -ErrorAction SilentlyContinue
        Write-RuleResult -RuleId "1.1.7" -Title "Ensure 'Store passwords using reversible encryption' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "1.1.7" -Title "Ensure 'Store passwords using reversible encryption' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 1.2.1 ──────────────────────────────────────
function Apply-CIS_1_2_1 {
    <#
    .SYNOPSIS
        Ensure 'Account lockout duration' is set to '15 or more minute(s)'
    .NOTES
        Category:       Account Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "1.2.1" -Title "Ensure 'Account lockout duration' is set to '15 or more minute(s)'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        $seceditExport = "$env:TEMP\secpol_export.cfg"
        $seceditImport = "$env:TEMP\secpol_import.cfg"
        secedit /export /cfg $seceditExport /quiet
        $content = Get-Content $seceditExport
        $content = $content -replace 'LockoutDuration\s*=\s*\d+', 'LockoutDuration = 15'
        if ($content -notmatch 'LockoutDuration') {
            $content = $content -replace '(\[System Access\])', "`$1`nLockoutDuration = 15"
        }
        $content | Set-Content $seceditImport -Force
        secedit /configure /db secedit.sdb /cfg $seceditImport /quiet
        Remove-Item $seceditExport, $seceditImport -Force -ErrorAction SilentlyContinue
        Write-RuleResult -RuleId "1.2.1" -Title "Ensure 'Account lockout duration' is set to '15 or more minute(s)'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "1.2.1" -Title "Ensure 'Account lockout duration' is set to '15 or more minute(s)'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 1.2.2 ──────────────────────────────────────
function Apply-CIS_1_2_2 {
    <#
    .SYNOPSIS
        Ensure 'Account lockout threshold' is set to '5 or fewer invalid logon attempt(s), but not 0'
    .NOTES
        Category:       Account Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "1.2.2" -Title "Ensure 'Account lockout threshold' is set to '5 or fewer invalid logon attempt(s), but not 0'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        $seceditExport = "$env:TEMP\secpol_export.cfg"
        $seceditImport = "$env:TEMP\secpol_import.cfg"
        secedit /export /cfg $seceditExport /quiet
        $content = Get-Content $seceditExport
        $content = $content -replace 'LockoutBadCount\s*=\s*\d+', 'LockoutBadCount = 5'
        if ($content -notmatch 'LockoutBadCount') {
            $content = $content -replace '(\[System Access\])', "`$1`nLockoutBadCount = 5"
        }
        $content | Set-Content $seceditImport -Force
        secedit /configure /db secedit.sdb /cfg $seceditImport /quiet
        Remove-Item $seceditExport, $seceditImport -Force -ErrorAction SilentlyContinue
        Write-RuleResult -RuleId "1.2.2" -Title "Ensure 'Account lockout threshold' is set to '5 or fewer invalid logon attempt(s), but not 0'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "1.2.2" -Title "Ensure 'Account lockout threshold' is set to '5 or fewer invalid logon attempt(s), but not 0'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 1.2.4 ──────────────────────────────────────
function Apply-CIS_1_2_4 {
    <#
    .SYNOPSIS
        Ensure 'Reset account lockout counter after' is set to '15 or more minute(s)'
    .NOTES
        Category:       Account Policies
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "1.2.4" -Title "Ensure 'Reset account lockout counter after' is set to '15 or more minute(s)'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        $seceditExport = "$env:TEMP\secpol_export.cfg"
        $seceditImport = "$env:TEMP\secpol_import.cfg"
        secedit /export /cfg $seceditExport /quiet
        $content = Get-Content $seceditExport
        $content = $content -replace 'ResetLockoutCount\s*=\s*\d+', 'ResetLockoutCount = 15'
        if ($content -notmatch 'ResetLockoutCount') {
            $content = $content -replace '(\[System Access\])', "`$1`nResetLockoutCount = 15"
        }
        $content | Set-Content $seceditImport -Force
        secedit /configure /db secedit.sdb /cfg $seceditImport /quiet
        Remove-Item $seceditExport, $seceditImport -Force -ErrorAction SilentlyContinue
        Write-RuleResult -RuleId "1.2.4" -Title "Ensure 'Reset account lockout counter after' is set to '15 or more minute(s)'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "1.2.4" -Title "Ensure 'Reset account lockout counter after' is set to '15 or more minute(s)'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.1 ──────────────────────────────────────
function Apply-CIS_5_1 {
    <#
    .SYNOPSIS
        Ensure 'Bluetooth Audio Gateway Service (BTAGService)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.1" -Title "Ensure 'Bluetooth Audio Gateway Service (BTAGService)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'BTAGService' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\BTAGService' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.1" -Title "Ensure 'Bluetooth Audio Gateway Service (BTAGService)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.1" -Title "Ensure 'Bluetooth Audio Gateway Service (BTAGService)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.10 ──────────────────────────────────────
function Apply-CIS_5_10 {
    <#
    .SYNOPSIS
        Ensure 'Microsoft FTP Service (FTPSVC)' is set to 'Disabled' or 'Not Installed'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.10" -Title "Ensure 'Microsoft FTP Service (FTPSVC)' is set to 'Disabled' or 'Not Installed'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'FTPSVC' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\FTPSVC' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.10" -Title "Ensure 'Microsoft FTP Service (FTPSVC)' is set to 'Disabled' or 'Not Installed'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.10" -Title "Ensure 'Microsoft FTP Service (FTPSVC)' is set to 'Disabled' or 'Not Installed'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.11 ──────────────────────────────────────
function Apply-CIS_5_11 {
    <#
    .SYNOPSIS
        Ensure 'Microsoft iSCSI Initiator Service (MSiSCSI)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.11" -Title "Ensure 'Microsoft iSCSI Initiator Service (MSiSCSI)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'MSiSCSI' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\MSiSCSI' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.11" -Title "Ensure 'Microsoft iSCSI Initiator Service (MSiSCSI)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.11" -Title "Ensure 'Microsoft iSCSI Initiator Service (MSiSCSI)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.12 ──────────────────────────────────────
function Apply-CIS_5_12 {
    <#
    .SYNOPSIS
        Ensure 'OpenSSH SSH Server (sshd)' is set to 'Disabled' or 'Not Installed'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.12" -Title "Ensure 'OpenSSH SSH Server (sshd)' is set to 'Disabled' or 'Not Installed'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'sshd' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\sshd' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.12" -Title "Ensure 'OpenSSH SSH Server (sshd)' is set to 'Disabled' or 'Not Installed'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.12" -Title "Ensure 'OpenSSH SSH Server (sshd)' is set to 'Disabled' or 'Not Installed'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.13 ──────────────────────────────────────
function Apply-CIS_5_13 {
    <#
    .SYNOPSIS
        Ensure 'Print Spooler (Spooler)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.13" -Title "Ensure 'Print Spooler (Spooler)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'Spooler' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\Spooler' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.13" -Title "Ensure 'Print Spooler (Spooler)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.13" -Title "Ensure 'Print Spooler (Spooler)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.14 ──────────────────────────────────────
function Apply-CIS_5_14 {
    <#
    .SYNOPSIS
        Ensure 'Problem Reports and Solutions Control Panel Support (wercplsupport)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.14" -Title "Ensure 'Problem Reports and Solutions Control Panel Support (wercplsupport)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'wercplsupport' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\wercplsupport' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.14" -Title "Ensure 'Problem Reports and Solutions Control Panel Support (wercplsupport)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.14" -Title "Ensure 'Problem Reports and Solutions Control Panel Support (wercplsupport)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.15 ──────────────────────────────────────
function Apply-CIS_5_15 {
    <#
    .SYNOPSIS
        Ensure 'Remote Access Auto Connection Manager (RasAuto)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.15" -Title "Ensure 'Remote Access Auto Connection Manager (RasAuto)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'RasAuto' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\RasAuto' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.15" -Title "Ensure 'Remote Access Auto Connection Manager (RasAuto)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.15" -Title "Ensure 'Remote Access Auto Connection Manager (RasAuto)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.16 ──────────────────────────────────────
function Apply-CIS_5_16 {
    <#
    .SYNOPSIS
        Ensure 'Remote Desktop Configuration (SessionEnv)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.16" -Title "Ensure 'Remote Desktop Configuration (SessionEnv)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'SessionEnv' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\SessionEnv' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.16" -Title "Ensure 'Remote Desktop Configuration (SessionEnv)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.16" -Title "Ensure 'Remote Desktop Configuration (SessionEnv)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.17 ──────────────────────────────────────
function Apply-CIS_5_17 {
    <#
    .SYNOPSIS
        Ensure 'Remote Desktop Services (TermService)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.17" -Title "Ensure 'Remote Desktop Services (TermService)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'TermService' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\TermService' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.17" -Title "Ensure 'Remote Desktop Services (TermService)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.17" -Title "Ensure 'Remote Desktop Services (TermService)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.18 ──────────────────────────────────────
function Apply-CIS_5_18 {
    <#
    .SYNOPSIS
        Ensure 'Remote Desktop Services UserMode Port Redirector (UmRdpService)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.18" -Title "Ensure 'Remote Desktop Services UserMode Port Redirector (UmRdpService)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'UmRdpService' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\UmRdpService' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.18" -Title "Ensure 'Remote Desktop Services UserMode Port Redirector (UmRdpService)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.18" -Title "Ensure 'Remote Desktop Services UserMode Port Redirector (UmRdpService)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.19 ──────────────────────────────────────
function Apply-CIS_5_19 {
    <#
    .SYNOPSIS
        Ensure 'Remote Procedure Call (RPC) Locator (RpcLocator)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.19" -Title "Ensure 'Remote Procedure Call (RPC) Locator (RpcLocator)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'RPC) Locator (RpcLocator' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\RPC) Locator (RpcLocator' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.19" -Title "Ensure 'Remote Procedure Call (RPC) Locator (RpcLocator)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.19" -Title "Ensure 'Remote Procedure Call (RPC) Locator (RpcLocator)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.2 ──────────────────────────────────────
function Apply-CIS_5_2 {
    <#
    .SYNOPSIS
        Ensure 'Bluetooth Support Service (bthserv)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.2" -Title "Ensure 'Bluetooth Support Service (bthserv)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'bthserv' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\bthserv' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.2" -Title "Ensure 'Bluetooth Support Service (bthserv)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.2" -Title "Ensure 'Bluetooth Support Service (bthserv)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.20 ──────────────────────────────────────
function Apply-CIS_5_20 {
    <#
    .SYNOPSIS
        Ensure 'Remote Registry (RemoteRegistry)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.20" -Title "Ensure 'Remote Registry (RemoteRegistry)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'RemoteRegistry' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\RemoteRegistry' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.20" -Title "Ensure 'Remote Registry (RemoteRegistry)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.20" -Title "Ensure 'Remote Registry (RemoteRegistry)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.21 ──────────────────────────────────────
function Apply-CIS_5_21 {
    <#
    .SYNOPSIS
        Ensure 'Routing and Remote Access (RemoteAccess)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.21" -Title "Ensure 'Routing and Remote Access (RemoteAccess)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'RemoteAccess' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\RemoteAccess' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.21" -Title "Ensure 'Routing and Remote Access (RemoteAccess)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.21" -Title "Ensure 'Routing and Remote Access (RemoteAccess)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.22 ──────────────────────────────────────
function Apply-CIS_5_22 {
    <#
    .SYNOPSIS
        Ensure 'Server (LanmanServer)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.22" -Title "Ensure 'Server (LanmanServer)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'LanmanServer' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\LanmanServer' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.22" -Title "Ensure 'Server (LanmanServer)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.22" -Title "Ensure 'Server (LanmanServer)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.23 ──────────────────────────────────────
function Apply-CIS_5_23 {
    <#
    .SYNOPSIS
        Ensure 'Simple TCP/IP Services (simptcp)' is set to 'Disabled' or 'Not Installed'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.23" -Title "Ensure 'Simple TCP/IP Services (simptcp)' is set to 'Disabled' or 'Not Installed'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'simptcp' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\simptcp' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.23" -Title "Ensure 'Simple TCP/IP Services (simptcp)' is set to 'Disabled' or 'Not Installed'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.23" -Title "Ensure 'Simple TCP/IP Services (simptcp)' is set to 'Disabled' or 'Not Installed'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.24 ──────────────────────────────────────
function Apply-CIS_5_24 {
    <#
    .SYNOPSIS
        Ensure 'SNMP Service (SNMP)' is set to 'Disabled' or 'Not Installed'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.24" -Title "Ensure 'SNMP Service (SNMP)' is set to 'Disabled' or 'Not Installed'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'SNMP' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\SNMP' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.24" -Title "Ensure 'SNMP Service (SNMP)' is set to 'Disabled' or 'Not Installed'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.24" -Title "Ensure 'SNMP Service (SNMP)' is set to 'Disabled' or 'Not Installed'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.25 ──────────────────────────────────────
function Apply-CIS_5_25 {
    <#
    .SYNOPSIS
        Ensure 'Special Administration Console Helper (sacsvr)' is set to 'Disabled' or 'Not Installed'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.25" -Title "Ensure 'Special Administration Console Helper (sacsvr)' is set to 'Disabled' or 'Not Installed'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'sacsvr' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\sacsvr' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.25" -Title "Ensure 'Special Administration Console Helper (sacsvr)' is set to 'Disabled' or 'Not Installed'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.25" -Title "Ensure 'Special Administration Console Helper (sacsvr)' is set to 'Disabled' or 'Not Installed'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.26 ──────────────────────────────────────
function Apply-CIS_5_26 {
    <#
    .SYNOPSIS
        Ensure 'SSDP Discovery (SSDPSRV)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.26" -Title "Ensure 'SSDP Discovery (SSDPSRV)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'SSDPSRV' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\SSDPSRV' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.26" -Title "Ensure 'SSDP Discovery (SSDPSRV)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.26" -Title "Ensure 'SSDP Discovery (SSDPSRV)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.27 ──────────────────────────────────────
function Apply-CIS_5_27 {
    <#
    .SYNOPSIS
        Ensure 'UPnP Device Host (upnphost)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.27" -Title "Ensure 'UPnP Device Host (upnphost)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'upnphost' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\upnphost' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.27" -Title "Ensure 'UPnP Device Host (upnphost)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.27" -Title "Ensure 'UPnP Device Host (upnphost)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.28 ──────────────────────────────────────
function Apply-CIS_5_28 {
    <#
    .SYNOPSIS
        Ensure 'Web Management Service (WMSvc)' is set to 'Disabled' or 'Not Installed'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.28" -Title "Ensure 'Web Management Service (WMSvc)' is set to 'Disabled' or 'Not Installed'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'WMSvc' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\WMSvc' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.28" -Title "Ensure 'Web Management Service (WMSvc)' is set to 'Disabled' or 'Not Installed'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.28" -Title "Ensure 'Web Management Service (WMSvc)' is set to 'Disabled' or 'Not Installed'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.29 ──────────────────────────────────────
function Apply-CIS_5_29 {
    <#
    .SYNOPSIS
        Ensure 'Windows Error Reporting Service (WerSvc)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.29" -Title "Ensure 'Windows Error Reporting Service (WerSvc)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'WerSvc' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\WerSvc' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.29" -Title "Ensure 'Windows Error Reporting Service (WerSvc)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.29" -Title "Ensure 'Windows Error Reporting Service (WerSvc)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.3 ──────────────────────────────────────
function Apply-CIS_5_3 {
    <#
    .SYNOPSIS
        Ensure 'Computer Browser (Browser)' is set to 'Disabled' or 'Not Installed'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.3" -Title "Ensure 'Computer Browser (Browser)' is set to 'Disabled' or 'Not Installed'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'Browser' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\Browser' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.3" -Title "Ensure 'Computer Browser (Browser)' is set to 'Disabled' or 'Not Installed'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.3" -Title "Ensure 'Computer Browser (Browser)' is set to 'Disabled' or 'Not Installed'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.30 ──────────────────────────────────────
function Apply-CIS_5_30 {
    <#
    .SYNOPSIS
        Ensure 'Windows Event Collector (Wecsvc)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.30" -Title "Ensure 'Windows Event Collector (Wecsvc)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'Wecsvc' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\Wecsvc' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.30" -Title "Ensure 'Windows Event Collector (Wecsvc)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.30" -Title "Ensure 'Windows Event Collector (Wecsvc)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.31 ──────────────────────────────────────
function Apply-CIS_5_31 {
    <#
    .SYNOPSIS
        Ensure 'Windows Media Player Network Sharing Service (WMPNetworkSvc)' is set to 'Disabled' or 'Not Installed'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.31" -Title "Ensure 'Windows Media Player Network Sharing Service (WMPNetworkSvc)' is set to 'Disabled' or 'Not Installed'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'WMPNetworkSvc' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\WMPNetworkSvc' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.31" -Title "Ensure 'Windows Media Player Network Sharing Service (WMPNetworkSvc)' is set to 'Disabled' or 'Not Installed'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.31" -Title "Ensure 'Windows Media Player Network Sharing Service (WMPNetworkSvc)' is set to 'Disabled' or 'Not Installed'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.32 ──────────────────────────────────────
function Apply-CIS_5_32 {
    <#
    .SYNOPSIS
        Ensure 'Windows Mobile Hotspot Service (icssvc)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.32" -Title "Ensure 'Windows Mobile Hotspot Service (icssvc)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'icssvc' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\icssvc' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.32" -Title "Ensure 'Windows Mobile Hotspot Service (icssvc)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.32" -Title "Ensure 'Windows Mobile Hotspot Service (icssvc)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.33 ──────────────────────────────────────
function Apply-CIS_5_33 {
    <#
    .SYNOPSIS
        Ensure 'Windows Push Notifications System Service (WpnService)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.33" -Title "Ensure 'Windows Push Notifications System Service (WpnService)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'WpnService' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\WpnService' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.33" -Title "Ensure 'Windows Push Notifications System Service (WpnService)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.33" -Title "Ensure 'Windows Push Notifications System Service (WpnService)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.34 ──────────────────────────────────────
function Apply-CIS_5_34 {
    <#
    .SYNOPSIS
        Ensure 'Windows PushToInstall Service (PushToInstall)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.34" -Title "Ensure 'Windows PushToInstall Service (PushToInstall)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'PushToInstall' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\PushToInstall' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.34" -Title "Ensure 'Windows PushToInstall Service (PushToInstall)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.34" -Title "Ensure 'Windows PushToInstall Service (PushToInstall)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.35 ──────────────────────────────────────
function Apply-CIS_5_35 {
    <#
    .SYNOPSIS
        Ensure 'Windows Remote Management (WS-Management) (WinRM)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.35" -Title "Ensure 'Windows Remote Management (WS-Management) (WinRM)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'WS-Management) (WinRM' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\WS-Management) (WinRM' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.35" -Title "Ensure 'Windows Remote Management (WS-Management) (WinRM)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.35" -Title "Ensure 'Windows Remote Management (WS-Management) (WinRM)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.36 ──────────────────────────────────────
function Apply-CIS_5_36 {
    <#
    .SYNOPSIS
        Ensure 'WinHTTP Web Proxy Auto-Discovery Service (WinHttpAutoProxySvc)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.36" -Title "Ensure 'WinHTTP Web Proxy Auto-Discovery Service (WinHttpAutoProxySvc)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'WinHttpAutoProxySvc' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\WinHttpAutoProxySvc' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.36" -Title "Ensure 'WinHTTP Web Proxy Auto-Discovery Service (WinHttpAutoProxySvc)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.36" -Title "Ensure 'WinHTTP Web Proxy Auto-Discovery Service (WinHttpAutoProxySvc)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.37 ──────────────────────────────────────
function Apply-CIS_5_37 {
    <#
    .SYNOPSIS
        Ensure 'World Wide Web Publishing Service (W3SVC)' is set to 'Disabled' or 'Not Installed'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.37" -Title "Ensure 'World Wide Web Publishing Service (W3SVC)' is set to 'Disabled' or 'Not Installed'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'W3SVC' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\W3SVC' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.37" -Title "Ensure 'World Wide Web Publishing Service (W3SVC)' is set to 'Disabled' or 'Not Installed'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.37" -Title "Ensure 'World Wide Web Publishing Service (W3SVC)' is set to 'Disabled' or 'Not Installed'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.38 ──────────────────────────────────────
function Apply-CIS_5_38 {
    <#
    .SYNOPSIS
        Ensure 'Xbox Accessory Management Service (XboxGipSvc)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.38" -Title "Ensure 'Xbox Accessory Management Service (XboxGipSvc)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'XboxGipSvc' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\XboxGipSvc' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.38" -Title "Ensure 'Xbox Accessory Management Service (XboxGipSvc)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.38" -Title "Ensure 'Xbox Accessory Management Service (XboxGipSvc)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.39 ──────────────────────────────────────
function Apply-CIS_5_39 {
    <#
    .SYNOPSIS
        Ensure 'Xbox Live Auth Manager (XblAuthManager)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.39" -Title "Ensure 'Xbox Live Auth Manager (XblAuthManager)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'XblAuthManager' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\XblAuthManager' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.39" -Title "Ensure 'Xbox Live Auth Manager (XblAuthManager)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.39" -Title "Ensure 'Xbox Live Auth Manager (XblAuthManager)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.4 ──────────────────────────────────────
function Apply-CIS_5_4 {
    <#
    .SYNOPSIS
        Ensure 'Downloaded Maps Manager (MapsBroker)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.4" -Title "Ensure 'Downloaded Maps Manager (MapsBroker)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'MapsBroker' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\MapsBroker' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.4" -Title "Ensure 'Downloaded Maps Manager (MapsBroker)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.4" -Title "Ensure 'Downloaded Maps Manager (MapsBroker)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.40 ──────────────────────────────────────
function Apply-CIS_5_40 {
    <#
    .SYNOPSIS
        Ensure 'Xbox Live Game Save (XblGameSave)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.40" -Title "Ensure 'Xbox Live Game Save (XblGameSave)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'XblGameSave' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\XblGameSave' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.40" -Title "Ensure 'Xbox Live Game Save (XblGameSave)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.40" -Title "Ensure 'Xbox Live Game Save (XblGameSave)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.41 ──────────────────────────────────────
function Apply-CIS_5_41 {
    <#
    .SYNOPSIS
        Ensure 'Xbox Live Networking Service (XboxNetApiSvc)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.41" -Title "Ensure 'Xbox Live Networking Service (XboxNetApiSvc)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'XboxNetApiSvc' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\XboxNetApiSvc' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.41" -Title "Ensure 'Xbox Live Networking Service (XboxNetApiSvc)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.41" -Title "Ensure 'Xbox Live Networking Service (XboxNetApiSvc)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.5 ──────────────────────────────────────
function Apply-CIS_5_5 {
    <#
    .SYNOPSIS
        Ensure 'GameInput Service (GameInputSvc)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.5" -Title "Ensure 'GameInput Service (GameInputSvc)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'GameInputSvc' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\GameInputSvc' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.5" -Title "Ensure 'GameInput Service (GameInputSvc)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.5" -Title "Ensure 'GameInput Service (GameInputSvc)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.6 ──────────────────────────────────────
function Apply-CIS_5_6 {
    <#
    .SYNOPSIS
        Ensure 'Geolocation Service (lfsvc)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.6" -Title "Ensure 'Geolocation Service (lfsvc)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'lfsvc' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\lfsvc' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.6" -Title "Ensure 'Geolocation Service (lfsvc)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.6" -Title "Ensure 'Geolocation Service (lfsvc)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.7 ──────────────────────────────────────
function Apply-CIS_5_7 {
    <#
    .SYNOPSIS
        Ensure 'IIS Admin Service (IISADMIN)' is set to 'Disabled' or 'Not Installed'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.7" -Title "Ensure 'IIS Admin Service (IISADMIN)' is set to 'Disabled' or 'Not Installed'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'IISADMIN' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\IISADMIN' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.7" -Title "Ensure 'IIS Admin Service (IISADMIN)' is set to 'Disabled' or 'Not Installed'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.7" -Title "Ensure 'IIS Admin Service (IISADMIN)' is set to 'Disabled' or 'Not Installed'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.8 ──────────────────────────────────────
function Apply-CIS_5_8 {
    <#
    .SYNOPSIS
        Ensure 'Infrared monitor service (irmon)' is set to 'Disabled' or 'Not Installed'
    .NOTES
        Category:       System Services
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.8" -Title "Ensure 'Infrared monitor service (irmon)' is set to 'Disabled' or 'Not Installed'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'irmon' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\irmon' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.8" -Title "Ensure 'Infrared monitor service (irmon)' is set to 'Disabled' or 'Not Installed'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.8" -Title "Ensure 'Infrared monitor service (irmon)' is set to 'Disabled' or 'Not Installed'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 5.9 ──────────────────────────────────────
function Apply-CIS_5_9 {
    <#
    .SYNOPSIS
        Ensure 'Link-Layer Topology Discovery Mapper (lltdsvc)' is set to 'Disabled'
    .NOTES
        Category:       System Services
        CIS Level:      2
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "5.9" -Title "Ensure 'Link-Layer Topology Discovery Mapper (lltdsvc)' is set to 'Disabled'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (Get-Service -Name 'lltdsvc' -ErrorAction SilentlyContinue) { Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Services\lltdsvc' -Name 'Start' -Value 4 -Type DWord -Force }
        Write-RuleResult -RuleId "5.9" -Title "Ensure 'Link-Layer Topology Discovery Mapper (lltdsvc)' is set to 'Disabled'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "5.9" -Title "Ensure 'Link-Layer Topology Discovery Mapper (lltdsvc)' is set to 'Disabled'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 9.2.1 ──────────────────────────────────────
function Apply-CIS_9_2_1 {
    <#
    .SYNOPSIS
        Ensure 'Windows Firewall: Private: Firewall state is set to On (recommended)'
    .NOTES
        Category:       Windows Firewall
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "9.2.1" -Title "Ensure 'Windows Firewall: Private: Firewall state is set to On (recommended)'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile' -Force }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile' -Name 'EnableFirewall' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "9.2.1" -Title "Ensure 'Windows Firewall: Private: Firewall state is set to On (recommended)'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "9.2.1" -Title "Ensure 'Windows Firewall: Private: Firewall state is set to On (recommended)'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 9.2.2 ──────────────────────────────────────
function Apply-CIS_9_2_2 {
    <#
    .SYNOPSIS
        Ensure 'Windows Firewall: Private: Inbound connections is set to Block (default)'
    .NOTES
        Category:       Windows Firewall
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "9.2.2" -Title "Ensure 'Windows Firewall: Private: Inbound connections is set to Block (default)'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile' -Force }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile' -Name 'DefaultInboundAction' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "9.2.2" -Title "Ensure 'Windows Firewall: Private: Inbound connections is set to Block (default)'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "9.2.2" -Title "Ensure 'Windows Firewall: Private: Inbound connections is set to Block (default)'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 9.2.3 ──────────────────────────────────────
function Apply-CIS_9_2_3 {
    <#
    .SYNOPSIS
        Ensure 'Windows Firewall: Private: Settings: Display a notification is set to No'
    .NOTES
        Category:       Windows Firewall
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "9.2.3" -Title "Ensure 'Windows Firewall: Private: Settings: Display a notification is set to No'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile' -Force }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile' -Name 'DisableNotifications' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "9.2.3" -Title "Ensure 'Windows Firewall: Private: Settings: Display a notification is set to No'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "9.2.3" -Title "Ensure 'Windows Firewall: Private: Settings: Display a notification is set to No'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 9.2.4 ──────────────────────────────────────
function Apply-CIS_9_2_4 {
    <#
    .SYNOPSIS
        Ensure 'Windows Firewall: Private: Logging: Name is set to %SystemRoot%\System32\logfiles\firewall\privatefw.log'
    .NOTES
        Category:       Windows Firewall
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "9.2.4" -Title "Ensure 'Windows Firewall: Private: Logging: Name is set to %SystemRoot%\System32\logfiles\firewall\privatefw.log'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile\Logging')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile\Logging' -Force }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile\Logging' -Name 'LogFilePath' -Value '%SystemRoot%\System32\logfiles\firewall\privatefw.log' -Type ExpandString -Force
        Write-RuleResult -RuleId "9.2.4" -Title "Ensure 'Windows Firewall: Private: Logging: Name is set to %SystemRoot%\System32\logfiles\firewall\privatefw.log'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "9.2.4" -Title "Ensure 'Windows Firewall: Private: Logging: Name is set to %SystemRoot%\System32\logfiles\firewall\privatefw.log'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 9.2.5 ──────────────────────────────────────
function Apply-CIS_9_2_5 {
    <#
    .SYNOPSIS
        Ensure 'Windows Firewall: Private: Logging: Size limit (KB) is set to 16,384 KB or greater'
    .NOTES
        Category:       Windows Firewall
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "9.2.5" -Title "Ensure 'Windows Firewall: Private: Logging: Size limit (KB) is set to 16,384 KB or greater'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile\Logging')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile\Logging' -Force }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile\Logging' -Name 'LogFileSize' -Value 16384 -Type DWord -Force
        Write-RuleResult -RuleId "9.2.5" -Title "Ensure 'Windows Firewall: Private: Logging: Size limit (KB) is set to 16,384 KB or greater'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "9.2.5" -Title "Ensure 'Windows Firewall: Private: Logging: Size limit (KB) is set to 16,384 KB or greater'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 9.2.6 ──────────────────────────────────────
function Apply-CIS_9_2_6 {
    <#
    .SYNOPSIS
        Ensure 'Windows Firewall: Private: Logging: Log dropped packets is set to Yes'
    .NOTES
        Category:       Windows Firewall
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "9.2.6" -Title "Ensure 'Windows Firewall: Private: Logging: Log dropped packets is set to Yes'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile\Logging')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile\Logging' -Force }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile\Logging' -Name 'LogDroppedPackets' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "9.2.6" -Title "Ensure 'Windows Firewall: Private: Logging: Log dropped packets is set to Yes'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "9.2.6" -Title "Ensure 'Windows Firewall: Private: Logging: Log dropped packets is set to Yes'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 9.2.7 ──────────────────────────────────────
function Apply-CIS_9_2_7 {
    <#
    .SYNOPSIS
        Ensure 'Windows Firewall: Private: Logging: Log successful connections is set to Yes'
    .NOTES
        Category:       Windows Firewall
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "9.2.7" -Title "Ensure 'Windows Firewall: Private: Logging: Log successful connections is set to Yes'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile\Logging')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile\Logging' -Force }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PrivateProfile\Logging' -Name 'LogSuccessfulConnections' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "9.2.7" -Title "Ensure 'Windows Firewall: Private: Logging: Log successful connections is set to Yes'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "9.2.7" -Title "Ensure 'Windows Firewall: Private: Logging: Log successful connections is set to Yes'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 9.3.1 ──────────────────────────────────────
function Apply-CIS_9_3_1 {
    <#
    .SYNOPSIS
        Ensure 'Windows Firewall: Public: Firewall state is set to On (recommended)'
    .NOTES
        Category:       Windows Firewall
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "9.3.1" -Title "Ensure 'Windows Firewall: Public: Firewall state is set to On (recommended)'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile' -Force }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile' -Name 'EnableFirewall' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "9.3.1" -Title "Ensure 'Windows Firewall: Public: Firewall state is set to On (recommended)'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "9.3.1" -Title "Ensure 'Windows Firewall: Public: Firewall state is set to On (recommended)'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 9.3.2 ──────────────────────────────────────
function Apply-CIS_9_3_2 {
    <#
    .SYNOPSIS
        Ensure 'Windows Firewall: Public: Inbound connections is set to Block (default)'
    .NOTES
        Category:       Windows Firewall
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "9.3.2" -Title "Ensure 'Windows Firewall: Public: Inbound connections is set to Block (default)'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile' -Force }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile' -Name 'DefaultInboundAction' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "9.3.2" -Title "Ensure 'Windows Firewall: Public: Inbound connections is set to Block (default)'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "9.3.2" -Title "Ensure 'Windows Firewall: Public: Inbound connections is set to Block (default)'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 9.3.3 ──────────────────────────────────────
function Apply-CIS_9_3_3 {
    <#
    .SYNOPSIS
        Ensure 'Windows Firewall: Public: Settings: Display a notification is set to No'
    .NOTES
        Category:       Windows Firewall
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "9.3.3" -Title "Ensure 'Windows Firewall: Public: Settings: Display a notification is set to No'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile' -Force }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile' -Name 'DisableNotifications' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "9.3.3" -Title "Ensure 'Windows Firewall: Public: Settings: Display a notification is set to No'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "9.3.3" -Title "Ensure 'Windows Firewall: Public: Settings: Display a notification is set to No'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 9.3.4 ──────────────────────────────────────
function Apply-CIS_9_3_4 {
    <#
    .SYNOPSIS
        Ensure 'Windows Firewall: Public: Settings: Apply local firewall rules is set to No'
    .NOTES
        Category:       Windows Firewall
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "9.3.4" -Title "Ensure 'Windows Firewall: Public: Settings: Apply local firewall rules is set to No'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile' -Force }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile' -Name 'AllowLocalPolicyMerge' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "9.3.4" -Title "Ensure 'Windows Firewall: Public: Settings: Apply local firewall rules is set to No'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "9.3.4" -Title "Ensure 'Windows Firewall: Public: Settings: Apply local firewall rules is set to No'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 9.3.5 ──────────────────────────────────────
function Apply-CIS_9_3_5 {
    <#
    .SYNOPSIS
        Ensure 'Windows Firewall: Public: Settings: Apply local connection security rules is set to No'
    .NOTES
        Category:       Windows Firewall
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "9.3.5" -Title "Ensure 'Windows Firewall: Public: Settings: Apply local connection security rules is set to No'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile' -Force }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile' -Name 'AllowLocalIPsecPolicyMerge' -Value 0 -Type DWord -Force
        Write-RuleResult -RuleId "9.3.5" -Title "Ensure 'Windows Firewall: Public: Settings: Apply local connection security rules is set to No'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "9.3.5" -Title "Ensure 'Windows Firewall: Public: Settings: Apply local connection security rules is set to No'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 9.3.6 ──────────────────────────────────────
function Apply-CIS_9_3_6 {
    <#
    .SYNOPSIS
        Ensure 'Windows Firewall: Public: Logging: Name is set to %SystemRoot%\System32\logfiles\firewall\publicfw.log'
    .NOTES
        Category:       Windows Firewall
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "9.3.6" -Title "Ensure 'Windows Firewall: Public: Logging: Name is set to %SystemRoot%\System32\logfiles\firewall\publicfw.log'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile\Logging')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile\Logging' -Force }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile\Logging' -Name 'LogFilePath' -Value '%SystemRoot%\System32\logfiles\firewall\publicfw.log' -Type ExpandString -Force
        Write-RuleResult -RuleId "9.3.6" -Title "Ensure 'Windows Firewall: Public: Logging: Name is set to %SystemRoot%\System32\logfiles\firewall\publicfw.log'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "9.3.6" -Title "Ensure 'Windows Firewall: Public: Logging: Name is set to %SystemRoot%\System32\logfiles\firewall\publicfw.log'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 9.3.7 ──────────────────────────────────────
function Apply-CIS_9_3_7 {
    <#
    .SYNOPSIS
        Ensure 'Windows Firewall: Public: Logging: Size limit (KB) is set to 16,384 KB or greater'
    .NOTES
        Category:       Windows Firewall
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "9.3.7" -Title "Ensure 'Windows Firewall: Public: Logging: Size limit (KB) is set to 16,384 KB or greater'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile\Logging')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile\Logging' -Force }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile\Logging' -Name 'LogFileSize' -Value 16384 -Type DWord -Force
        Write-RuleResult -RuleId "9.3.7" -Title "Ensure 'Windows Firewall: Public: Logging: Size limit (KB) is set to 16,384 KB or greater'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "9.3.7" -Title "Ensure 'Windows Firewall: Public: Logging: Size limit (KB) is set to 16,384 KB or greater'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 9.3.8 ──────────────────────────────────────
function Apply-CIS_9_3_8 {
    <#
    .SYNOPSIS
        Ensure 'Windows Firewall: Public: Logging: Log dropped packets is set to Yes'
    .NOTES
        Category:       Windows Firewall
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "9.3.8" -Title "Ensure 'Windows Firewall: Public: Logging: Log dropped packets is set to Yes'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile\Logging')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile\Logging' -Force }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile\Logging' -Name 'LogDroppedPackets' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "9.3.8" -Title "Ensure 'Windows Firewall: Public: Logging: Log dropped packets is set to Yes'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "9.3.8" -Title "Ensure 'Windows Firewall: Public: Logging: Log dropped packets is set to Yes'" -Status "FAIL" -Details $_.Exception.Message
    }
}

# ── CIS 9.3.9 ──────────────────────────────────────
function Apply-CIS_9_3_9 {
    <#
    .SYNOPSIS
        Ensure 'Windows Firewall: Public: Logging: Log successful connections is set to Yes'
    .NOTES
        Category:       Windows Firewall
        CIS Level:      1
        Requires Admin: True
        Requires Reboot:False
    #>
    try {
        if ($WhatIf) {
            Write-RuleResult -RuleId "9.3.9" -Title "Ensure 'Windows Firewall: Public: Logging: Log successful connections is set to Yes'" -Status "SKIPPED" -Details "WhatIf mode"
            return
        }
        if (-not (Test-Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile\Logging')) { New-Item -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile\Logging' -Force }; Set-ItemProperty -Path 'HKLM:\SOFTWARE\Policies\Microsoft\WindowsFirewall\PublicProfile\Logging' -Name 'LogSuccessfulConnections' -Value 1 -Type DWord -Force
        Write-RuleResult -RuleId "9.3.9" -Title "Ensure 'Windows Firewall: Public: Logging: Log successful connections is set to Yes'" -Status "APPLIED" -Details "Successfully applied"
    }
    catch {
        Write-RuleResult -RuleId "9.3.9" -Title "Ensure 'Windows Firewall: Public: Logging: Log successful connections is set to Yes'" -Status "FAIL" -Details $_.Exception.Message
    }
}


# ══════════════════════════════════════════════════════════════
# Main Execution
# ══════════════════════════════════════════════════════════════

Write-Host ""
Write-Host "================================================================" -ForegroundColor Cyan
Write-Host "  CIS Windows Hardening – 248 Rules" -ForegroundColor Cyan
Write-Host "================================================================" -ForegroundColor Cyan
Write-Host ""

Apply-CIS_2_3_1_1
Apply-CIS_2_3_1_2
Apply-CIS_2_3_1_3
Apply-CIS_2_3_1_4
Apply-CIS_2_3_10_1
Apply-CIS_2_3_10_10
Apply-CIS_2_3_10_11
Apply-CIS_2_3_10_12
Apply-CIS_2_3_10_2
Apply-CIS_2_3_10_3
Apply-CIS_2_3_10_4
Apply-CIS_2_3_10_5
Apply-CIS_2_3_10_6
Apply-CIS_2_3_10_7
Apply-CIS_2_3_10_8
Apply-CIS_2_3_10_9
Apply-CIS_2_3_11_1
Apply-CIS_2_3_11_10
Apply-CIS_2_3_11_11
Apply-CIS_2_3_11_12
Apply-CIS_2_3_11_13
Apply-CIS_2_3_11_2
Apply-CIS_2_3_11_3
Apply-CIS_2_3_11_4
Apply-CIS_2_3_11_5
Apply-CIS_2_3_11_7
Apply-CIS_2_3_11_8
Apply-CIS_2_3_11_9
Apply-CIS_2_3_14_1
Apply-CIS_2_3_15_1
Apply-CIS_2_3_15_2
Apply-CIS_2_3_17_1
Apply-CIS_2_3_17_2
Apply-CIS_2_3_17_3
Apply-CIS_2_3_17_4
Apply-CIS_2_3_17_5
Apply-CIS_2_3_17_6
Apply-CIS_2_3_17_7
Apply-CIS_2_3_17_8
Apply-CIS_2_3_2_1
Apply-CIS_2_3_2_2
Apply-CIS_2_3_4_1
Apply-CIS_2_3_7_1
Apply-CIS_2_3_7_2
Apply-CIS_2_3_7_3
Apply-CIS_2_3_7_4
Apply-CIS_2_3_7_5
Apply-CIS_2_3_7_6
Apply-CIS_2_3_7_7
Apply-CIS_2_3_7_8
Apply-CIS_2_3_8_1
Apply-CIS_2_3_8_2
Apply-CIS_2_3_8_3
Apply-CIS_2_3_9_1
Apply-CIS_2_3_9_2
Apply-CIS_2_3_9_3
Apply-CIS_2_3_9_4
Apply-CIS_2_3_9_5
Apply-CIS_17_1_1
Apply-CIS_17_2_1
Apply-CIS_17_2_2
Apply-CIS_17_2_3
Apply-CIS_17_3_1
Apply-CIS_17_3_2
Apply-CIS_17_5_1
Apply-CIS_17_5_2
Apply-CIS_17_5_3
Apply-CIS_17_5_4
Apply-CIS_17_5_5
Apply-CIS_17_5_6
Apply-CIS_17_6_1
Apply-CIS_17_6_2
Apply-CIS_17_6_3
Apply-CIS_17_6_4
Apply-CIS_17_7_1
Apply-CIS_17_7_2
Apply-CIS_17_7_3
Apply-CIS_17_7_4
Apply-CIS_17_7_5
Apply-CIS_17_8_1
Apply-CIS_17_9_1
Apply-CIS_17_9_2
Apply-CIS_17_9_3
Apply-CIS_17_9_4
Apply-CIS_17_9_5
Apply-CIS_18_1_1_1
Apply-CIS_18_1_1_2
Apply-CIS_18_1_2_2
Apply-CIS_18_1_3
Apply-CIS_18_4_1
Apply-CIS_18_4_2
Apply-CIS_18_4_3
Apply-CIS_18_4_4
Apply-CIS_18_4_5
Apply-CIS_18_4_6
Apply-CIS_18_5_1
Apply-CIS_18_5_10
Apply-CIS_18_5_11
Apply-CIS_18_5_12
Apply-CIS_18_5_13
Apply-CIS_18_5_2
Apply-CIS_18_5_3
Apply-CIS_18_5_4
Apply-CIS_18_5_5
Apply-CIS_18_5_6
Apply-CIS_18_5_7
Apply-CIS_18_5_8
Apply-CIS_18_5_9
Apply-CIS_18_6_10_2
Apply-CIS_18_6_11_2
Apply-CIS_18_6_11_3
Apply-CIS_18_6_14_1
Apply-CIS_18_6_19_2_1
Apply-CIS_18_6_20_1
Apply-CIS_18_6_20_2
Apply-CIS_18_6_21_1
Apply-CIS_18_6_23_2_1
Apply-CIS_18_6_4_1
Apply-CIS_18_6_4_2
Apply-CIS_18_6_5_1
Apply-CIS_18_6_7_1
Apply-CIS_18_6_7_2
Apply-CIS_18_6_7_3
Apply-CIS_18_6_7_4
Apply-CIS_18_6_7_5
Apply-CIS_18_6_7_6
Apply-CIS_18_6_7_7
Apply-CIS_18_6_8_1
Apply-CIS_18_6_8_2
Apply-CIS_18_6_8_3
Apply-CIS_18_6_8_4
Apply-CIS_18_6_8_5
Apply-CIS_18_6_8_6
Apply-CIS_18_6_8_7
Apply-CIS_18_6_9_1
Apply-CIS_18_6_9_2
Apply-CIS_18_7_1
Apply-CIS_18_7_10
Apply-CIS_18_7_11
Apply-CIS_18_7_12
Apply-CIS_18_7_13
Apply-CIS_18_7_2
Apply-CIS_18_7_3
Apply-CIS_18_7_4
Apply-CIS_18_7_5
Apply-CIS_18_7_6
Apply-CIS_18_7_7
Apply-CIS_18_7_8
Apply-CIS_18_7_9
Apply-CIS_18_8_1_1
Apply-CIS_18_8_2
Apply-CIS_18_9_13_1
Apply-CIS_18_9_19_2
Apply-CIS_18_9_20_1_1
Apply-CIS_18_9_20_1_10
Apply-CIS_18_9_20_1_11
Apply-CIS_18_9_20_1_12
Apply-CIS_18_9_20_1_13
Apply-CIS_18_9_20_1_14
Apply-CIS_18_9_20_1_2
Apply-CIS_18_9_20_1_3
Apply-CIS_18_9_20_1_4
Apply-CIS_18_9_20_1_5
Apply-CIS_18_9_20_1_6
Apply-CIS_18_9_20_1_7
Apply-CIS_18_9_20_1_8
Apply-CIS_18_9_20_1_9
Apply-CIS_18_9_3_1
Apply-CIS_18_9_4_1
Apply-CIS_18_9_4_2
Apply-CIS_18_9_5_1
Apply-CIS_18_9_5_2
Apply-CIS_18_9_5_3
Apply-CIS_18_9_5_4
Apply-CIS_18_9_5_5
Apply-CIS_18_9_5_6
Apply-CIS_18_9_5_7
Apply-CIS_18_9_7_1_1
Apply-CIS_18_9_7_1_2
Apply-CIS_18_9_7_1_3
Apply-CIS_18_9_7_2
Apply-CIS_1_1_1
Apply-CIS_1_1_2
Apply-CIS_1_1_3
Apply-CIS_1_1_4
Apply-CIS_1_1_5
Apply-CIS_1_1_6
Apply-CIS_1_1_7
Apply-CIS_1_2_1
Apply-CIS_1_2_2
Apply-CIS_1_2_4
Apply-CIS_5_1
Apply-CIS_5_10
Apply-CIS_5_11
Apply-CIS_5_12
Apply-CIS_5_13
Apply-CIS_5_14
Apply-CIS_5_15
Apply-CIS_5_16
Apply-CIS_5_17
Apply-CIS_5_18
Apply-CIS_5_19
Apply-CIS_5_2
Apply-CIS_5_20
Apply-CIS_5_21
Apply-CIS_5_22
Apply-CIS_5_23
Apply-CIS_5_24
Apply-CIS_5_25
Apply-CIS_5_26
Apply-CIS_5_27
Apply-CIS_5_28
Apply-CIS_5_29
Apply-CIS_5_3
Apply-CIS_5_30
Apply-CIS_5_31
Apply-CIS_5_32
Apply-CIS_5_33
Apply-CIS_5_34
Apply-CIS_5_35
Apply-CIS_5_36
Apply-CIS_5_37
Apply-CIS_5_38
Apply-CIS_5_39
Apply-CIS_5_4
Apply-CIS_5_40
Apply-CIS_5_41
Apply-CIS_5_5
Apply-CIS_5_6
Apply-CIS_5_7
Apply-CIS_5_8
Apply-CIS_5_9
Apply-CIS_9_2_1
Apply-CIS_9_2_2
Apply-CIS_9_2_3
Apply-CIS_9_2_4
Apply-CIS_9_2_5
Apply-CIS_9_2_6
Apply-CIS_9_2_7
Apply-CIS_9_3_1
Apply-CIS_9_3_2
Apply-CIS_9_3_3
Apply-CIS_9_3_4
Apply-CIS_9_3_5
Apply-CIS_9_3_6
Apply-CIS_9_3_7
Apply-CIS_9_3_8
Apply-CIS_9_3_9

# Summary
Write-Host ""
Write-Host "================================================================" -ForegroundColor Cyan
$applied = ($Script:Results | Where-Object Status -eq "APPLIED").Count
$failed  = ($Script:Results | Where-Object Status -eq "FAIL").Count
$skipped = ($Script:Results | Where-Object Status -eq "SKIPPED").Count
Write-Host "Applied: $applied  |  Failed: $failed  |  Skipped: $skipped" -ForegroundColor White
Write-Host "================================================================" -ForegroundColor Cyan

